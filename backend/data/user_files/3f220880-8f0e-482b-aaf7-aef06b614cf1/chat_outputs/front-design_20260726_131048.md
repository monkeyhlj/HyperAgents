我来为你设计一个高端酒类网站首页，采用深色奢华风格，配以金色点缀和精美的酒类图片。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR · 臻藏佳酿</title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0a;
    --bg-2: #141414;
    --gold: #c9a96e;
    --gold-light: #e8d5a8;
    --gold-dark: #8a7444;
    --cream: #f5f0e8;
    --text: #d4d4d4;
    --muted: #8a8a8a;
  }
  * { -webkit-font-smoothing: antialiased; }
  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Noto Serif SC', serif;
    overflow-x: hidden;
  }
  .font-display { font-family: 'Cormorant Garamond', serif; }
  .text-gold { color: var(--gold); }
  .text-cream { color: var(--cream); }
  .bg-gold { background-color: var(--gold); }
  .border-gold { border-color: var(--gold); }

  /* 金色渐变文字 */
  .gold-text {
    background: linear-gradient(135deg, #e8d5a8 0%, #c9a96e 40%, #8a7444 60%, #e8d5a8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Hero 背景 */
  .hero-bg {
    background:
      radial-gradient(ellipse at 70% 50%, rgba(201,169,110,0.15) 0%, transparent 50%),
      radial-gradient(ellipse at 20% 80%, rgba(139,0,0,0.1) 0%, transparent 50%),
      linear-gradient(180deg, #0a0a0a 0%, #141414 100%);
  }

  /* 玻璃质感 */
  .glass {
    background: rgba(20,20,20,0.6);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(201,169,110,0.15);
  }

  /* 金色按钮 */
  .btn-gold {
    background: linear-gradient(135deg, #c9a96e 0%, #8a7444 100%);
    color: #0a0a0a;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  .btn-gold::before {
    content: '';
    position: absolute;
    top: 0; left: -100%;
    width: 100%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.6s ease;
  }
  .btn-gold:hover::before { left: 100%; }
  .btn-gold:hover {
    box-shadow: 0 10px 40px rgba(201,169,110,0.4);
    transform: translateY(-2px);
  }

  /* 描边按钮 */
  .btn-outline {
    border: 1px solid rgba(201,169,110,0.4);
    color: var(--gold);
    transition: all 0.4s ease;
  }
  .btn-outline:hover {
    border-color: var(--gold);
    background: rgba(201,169,110,0.08);
    color: var(--gold-light);
  }

  /* 产品卡片 */
  .product-card {
    background: linear-gradient(180deg, #161616 0%, #0e0e0e 100%);
    border: 1px solid rgba(201,169,110,0.08);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }
  .product-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 0%, rgba(201,169,110,0.1), transparent 60%);
    opacity: 0;
    transition: opacity 0.5s ease;
  }
  .product-card:hover {
    border-color: rgba(201,169,110,0.3);
    transform: translateY(-8px);
  }
  .product-card:hover::after { opacity: 1; }
  .product-card:hover .product-img {
    transform: scale(1.08);
  }
  .product-img {
    transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* 分隔线装饰 */
  .divider-gold {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  /* 滚动揭示动画 */
  .reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 1s ease, transform 1s ease;
  }
  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }

  /* 导航栏滚动效果 */
  .nav-scrolled {
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(201,169,110,0.1);
  }

  /* 烟雾飘动动画 */
  @keyframes drift {
    0%, 100% { transform: translate(0, 0) scale(1); opacity: 0.3; }
    50% { transform: translate(30px, -20px) scale(1.1); opacity: 0.15; }
  }
  .smoke {
    animation: drift 12s ease-in-out infinite;
  }

  /* 光晕脉动 */
  @keyframes glow-pulse {
    0%, 100% { opacity: 0.4; }
    50% { opacity: 0.7; }
  }
  .glow-pulse { animation: glow-pulse 4s ease-in-out infinite; }

  /* 慢速旋转 */
  @keyframes slow-spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .slow-spin { animation: slow-spin 60s linear infinite; }

  /* 数字滚动 */
  .stat-num {
    font-variant-numeric: tabular-nums;
  }

  /* 选中文字 */
  ::selection { background: var(--gold); color: #0a0a0a; }

  /* 滚动条 */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: #0a0a0a; }
  ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 3px; }

  /* 标签 */
  .tag {
    font-size: 10px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--gold);
    border: 1px solid rgba(201,169,110,0.3);
    padding: 4px 12px;
    border-radius: 999px;
  }

  /* 时间线 */
  .timeline-line {
    background: linear-gradient(180deg, transparent, var(--gold) 20%, var(--gold) 80%, transparent);
  }

  /* 悬停显示 */
  .product-card .reveal-detail {
    opacity: 0;
    transform: translateY(10px);
    transition: all 0.4s ease;
  }
  .product-card:hover .reveal-detail {
    opacity: 1;
    transform: translateY(0);
  }
</style>
</head>
<body class="min-h-screen">

<!-- 导航栏 -->
<nav id="navbar" class="fixed top-0 left-0 right-0 z-50 transition-all duration-500">
  <div class="max-w-7xl mx-auto px-6 lg:px-12 py-5 flex items-center justify-between">
    <a href="#" class="flex items-center gap-3">
      <div class="w-9 h-9 border border-gold flex items-center justify-center rotate-45">
        <div class="w-4 h-4 bg-gold"></div>
      </div>
      <span class="font-display text-2xl tracking-widest text-cream">MAISON NOIR</span>
    </a>
    <div class="hidden lg:flex items-center gap-10 text-sm tracking-wider">
      <a href="#collection" class="hover:text-gold transition-colors">臻藏系列</a>
      <a href="#story" class="hover:text-gold transition-colors">品牌故事</a>
      <a href="#craft" class="hover:text-gold transition-colors">酿造工艺</a>
      <a href="#journal" class="hover:text-gold transition-colors">品鉴手记</a>
      <a href="#contact" class="hover:text-gold transition-colors">联系我们</a

使用的 Skill: front-design