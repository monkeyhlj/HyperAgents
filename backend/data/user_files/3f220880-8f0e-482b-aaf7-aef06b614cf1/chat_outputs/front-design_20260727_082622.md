<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON FLEUR — 高定花艺 · 永恒之约</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600&family=Italiana&display=swap" rel="stylesheet">
<style>
  :root{
    --velvet:#1a0d12;
    --velvet-deep:#0f0709;
    --burgundy:#5c1a2b;
    --burgundy-soft:#7a2438;
    --rose:#c97064;
    --rose-pale:#e8b4a8;
    --gold:#c9a55c;
    --gold-bright:#e6c982;
    --gold-soft:#8a6f3a;
    --cream:#f4ebe0;
    --ink:#1a0d12;
    --glass:rgba(255,255,255,0.04);
    --glass-border:rgba(201,165,92,0.18);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    background:var(--velvet-deep);
    color:var(--cream);
    font-family:'Noto Serif SC',serif;
    font-weight:300;
    line-height:1.7;
    overflow-x:hidden;
    -webkit-font-smoothing:antialiased;
  }
  ::selection{background:var(--gold);color:var(--velvet-deep)}

  /* ---------- NAV ---------- */
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    padding:22px 48px;
    display:flex;align-items:center;justify-content:space-between;
    background:linear-gradient(180deg,rgba(15,7,9,0.85) 0%,rgba(15,7,9,0) 100%);
    backdrop-filter:blur(6px);
    transition:padding .4s ease, background .4s ease;
  }
  .nav.scrolled{padding:14px 48px;background:rgba(15,7,9,0.92)}
  .nav__brand{
    font-family:'Italiana',serif;
    font-size:22px;letter-spacing:.42em;
    color:var(--cream);
  }
  .nav__brand span{color:var(--gold)}
  .nav__links{display:flex;gap:38px;list-style:none}
  .nav__links a{
    color:var(--cream);text-decoration:none;
    font-size:12px;letter-spacing:.28em;text-transform:uppercase;
    font-family:'Cormorant Garamond',serif;font-weight:500;
    opacity:.78;transition:opacity .3s, color .3s;
    position:relative;
  }
  .nav__links a:hover{opacity:1;color:var(--gold-bright)}
  .nav__links a::after{
    content:'';position:absolute;left:0;bottom:-6px;width:0;height:1px;
    background:var(--gold);transition:width .4s ease;
  }
  .nav__links a:hover::after{width:100%}
  .nav__cta{
    font-size:11px;letter-spacing:.3em;text-transform:uppercase;
    color:var(--velvet-deep);background:var(--gold);
    padding:10px 22px;border:none;cursor:pointer;
    font-family:'Cormorant Garamond',serif;font-weight:600;
    transition:all .3s;
  }
  .nav__cta:hover{background:var(--gold-bright);letter-spacing:.36em}

  /* ---------- HERO ---------- */
  .hero{
    position:relative;
    min-height:100vh;
    display:flex;align-items:center;justify-content:center;
    overflow:hidden;
    background:
      radial-gradient(ellipse at 30% 40%, rgba(92,26,43,0.55) 0%, transparent 55%),
      radial-gradient(ellipse at 75% 65%, rgba(201,165,92,0.12) 0%, transparent 50%),
      linear-gradient(180deg, var(--velvet-deep) 0%, var(--velvet) 50%, var(--velvet-deep) 100%);
  }
  .hero__noise{
    position:absolute;inset:0;opacity:.06;pointer-events:none;
    background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  .hero__petals{
    position:absolute;inset:0;pointer-events:none;overflow:hidden;
  }
  .petal{
    position:absolute;width:14px;height:14px;
    background:radial-gradient(ellipse at 30% 30%, var(--rose-pale), var(--rose) 60%, transparent 75%);
    border-radius:50% 10% 50% 10%;
    opacity:0;
    animation:fall linear infinite;
    filter:blur(.5px);
  }
  @keyframes fall{
    0%{transform:translateY(-10vh) rotate(0deg) scale(.8);opacity:0}
    8%{opacity:.7}
    50%{opacity:.5}
    100%{transform:translateY(110vh) rotate(540deg) scale(1.1);opacity:0}
  }
  .hero__inner{
    position:relative;z-index:5;
    text-align:center;
    padding:0 24px;
    max-width:1100px;
  }
  .hero__eyebrow{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;font-weight:300;
    font-size:15px;letter-spacing:.5em;
    color:var(--gold);
    text-transform:uppercase;
    margin-bottom:36px;
    opacity:0;animation:fadeUp 1.2s .3s forwards;
  }
  .hero__eyebrow::before,
  .hero__eyebrow::after{
    content:'';display:inline-block;
    width:48px;height:1px;
    background:var(--gold-soft);
    vertical-align:middle;
    margin:0 18px;
  }
  .hero__title{
    font-family:'Italiana',serif;
    font-size:clamp(56px, 9vw, 132px);
    line-height:.95;
    letter-spacing:.04em;
    color:var(--cream);
    font-weight:400;
    margin-bottom:8px;
    opacity:0;animation:fadeUp 1.4s .5s forwards;
  }
  .hero__title .amp{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;
    font-weight:300;
    color:var(--gold);
    font-size:.7em;
    vertical-align:middle;
    margin:0 .12em;
  }
  .hero__sub{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;font-weight:300;
    font-size:clamp(20px,2.4vw,30px);
    color:var(--rose-pale);
    letter-spacing:.08em;
    margin-bottom:48px;
    opacity:0;animation:fadeUp 1.2s .8s forwards;
  }
  .hero__desc{
    max-width:560px;margin:0 auto 56px;
    font-size:15px;line-height:1.9;color:rgba(244,235,224,0.62);
    letter-spacing:.04em;
    opacity:0;animation:fadeUp 1.2s 1.1s forwards;
  }
  .hero__actions{
    display:flex;gap:24px;justify-content:center;align-items:center;
    opacity:0;animation:fadeUp 1.2s 1.4s forwards;
  }
  .btn{
    display:inline-flex;align-items:center;gap:12px;
    font-family:'Cormorant Garamond',serif;
    font-size:13px;letter-spacing:.32em;text-transform:uppercase;
    padding:16px 38px;cursor:pointer;text-decoration:none;
    transition:all .4s ease;border:none;
  }
  .btn--gold{
    background:var(--gold);color:var(--velvet-deep);font-weight:600;
    box-shadow:0 0 0 0 rgba(201,165,92,0.5);
  }
  .btn--gold:hover{
    background:var(--gold-bright);
    box-shadow:0 12px 40px -8px rgba(201,165,92,0.5);
    transform:translateY(-2px);
  }
  .btn--ghost{
    background:transparent;color:var(--cream);
    border:1px solid var(--glass-border);
  }
  .btn--ghost:hover{
    border-color:var(--gold);color:var(--gold-bright);
    background:var(--glass);
  }
  .btn__dot{width:5px;height:5px;border-radius:50%;background:currentColor;opacity:.6}

  /* signature ring */
  .hero__ring{
    position:absolute;
    top:50%;left:50%;
    transform:translate(-50%,-50%);
    width:min(680px,90vw);height:min(680px,90vw);
    z-index:1;pointer-events:none;
    opacity:0;animation:fadeIn 2s .6s forwards;
  }
  .ring__circle{
    position:absolute;inset:0;
    border:1px solid rgba(201,165,92,0.16);
    border-radius:50%;
  }
  .ring__circle--inner{
    inset:18%;
    border-color:rgba(201,165,92,0.1);
    border-style:dashed;
    animation:spin 80s linear infinite;
  }
  .ring__circle--outer{
    inset:-8%;
    border-color:rgba(201,165,92,0.08);
    animation:spin 120s linear infinite reverse;
  }
  .ring__text{
    position:absolute;top:50%;left:50%;
    transform:translate(-50%,-50%);
    width:100%;height:100%;
  }
  .ring__text svg{width:100%;height:100%}
  .ring__text text{
    font-family:'Cormorant Garamond',serif;
    font-size:11px;letter-spacing:.4em;
    fill:var(--gold-soft);
    text-transform:uppercase;
  }
  .ring__bloom{
    position:absolute;top:50%;left:50%;
    transform:translate(-50%,-50%);
    width:120px;height:120px;
    opacity:.5;
  }
  .ring__bloom svg{width:100%;height:100%}
  @keyframes spin{to{transform:rotate(360deg)}}
  @keyframes fadeUp{
    from{opacity:0;transform:translateY(28px)}
    to{opacity:1;transform:translateY(0)}
  }
  @keyframes fadeIn{to{opacity:1}}

  .hero__scroll{
    position:absolute;bottom:36px;left:50%;
    transform:translateX(-50%);
    z-index:5;
}
</style>
</html>