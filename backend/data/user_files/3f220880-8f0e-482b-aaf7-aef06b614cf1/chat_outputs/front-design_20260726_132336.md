我为你设计一个高端酒类电商网站首页，采用深色奢华风格，配以金色点缀和优雅的衬线字体。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR · 臻藏佳酿</title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@300;400;500;600;700&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0807;
    --bg-2: #14100d;
    --bg-3: #1c1714;
    --gold: #c9a961;
    --gold-light: #e6c98a;
    --gold-dark: #8a7340;
    --cream: #f5efe0;
    --muted: #8a7e6e;
    --border: rgba(201, 169, 97, 0.18);
  }
  * { -webkit-font-smoothing: antialiased; }
  body {
    background: var(--bg);
    color: var(--cream);
    font-family: 'Jost', 'Noto Serif SC', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
  }
  .font-serif { font-family: 'Cormorant Garamond', 'Noto Serif SC', serif; }
  .font-cn { font-family: 'Noto Serif SC', serif; }
  .text-gold { color: var(--gold); }
  .text-gold-light { color: var(--gold-light); }
  .text-cream { color: var(--cream); }
  .text-muted { color: var(--muted); }
  .bg-card { background: var(--bg-2); }
  .border-gold { border-color: var(--border); }

  /* 金色渐变文字 */
  .gold-text {
    background: linear-gradient(135deg, #e6c98a 0%, #c9a961 40%, #8a7340 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Hero 背景 */
  .hero-bg {
    background:
      radial-gradient(ellipse at 20% 30%, rgba(201, 169, 97, 0.12) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 70%, rgba(138, 115, 64, 0.08) 0%, transparent 50%),
      linear-gradient(180deg, #0a0807 0%, #14100d 50%, #0a0807 100%);
  }

  /* 噪点纹理 */
  .noise::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.4'/%3E%3C/svg%3E");
    opacity: 0.06;
    pointer-events: none;
    mix-blend-mode: overlay;
  }

  /* 金色按钮 */
  .btn-gold {
    background: linear-gradient(135deg, #c9a961 0%, #8a7340 100%);
    color: #0a0807;
    border: 1px solid #c9a961;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    letter-spacing: 0.15em;
  }
  .btn-gold:hover {
    background: linear-gradient(135deg, #e6c98a 0%, #c9a961 100%);
    box-shadow: 0 0 30px rgba(201, 169, 97, 0.35);
    transform: translateY(-2px);
  }
  .btn-outline {
    border: 1px solid var(--border);
    color: var(--cream);
    transition: all 0.4s ease;
    letter-spacing: 0.15em;
  }
  .btn-outline:hover {
    border-color: var(--gold);
    color: var(--gold-light);
    background: rgba(201, 169, 97, 0.05);
  }

  /* 装饰线 */
  .divider-gold {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  /* 卡片悬浮 */
  .product-card {
    background: linear-gradient(180deg, var(--bg-2) 0%, var(--bg-3) 100%);
    border: 1px solid var(--border);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .product-card:hover {
    border-color: rgba(201, 169, 97, 0.5);
    transform: translateY(-6px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5), 0 0 40px rgba(201, 169, 97, 0.08);
  }
  .product-card:hover .product-img {
    transform: scale(1.08);
    filter: brightness(1.1);
  }
  .product-img {
    transition: all 0.7s cubic-bezier(0.4, 0, 0.2, 1);
    filter: brightness(0.95);
  }

  /* 入场动画 */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  .reveal {
    opacity: 0;
    animation: fadeUp 1s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }
  .reveal-fade {
    opacity: 0;
    animation: fadeIn 1.4s ease forwards;
  }

  /* 光晕浮动 */
  @keyframes floatGlow {
    0%, 100% { transform: translate(0, 0); opacity: 0.4; }
    50% { transform: translate(20px, -20px); opacity: 0.6; }
  }
  .glow-orb {
    animation: floatGlow 8s ease-in-out infinite;
  }

  /* 标签 */
  .tag {
    border: 1px solid var(--border);
    color: var(--gold);
    font-size: 10px;
    letter-spacing: 0.2em;
    padding: 4px 10px;
    border-radius: 2px;
  }

  /* 滚动指示 */
  @keyframes scrollDown {
    0% { transform: translateY(0); opacity: 0; }
    50% { opacity: 1; }
    100% { transform: translateY(12px); opacity: 0; }
  }
  .scroll-dot { animation: scrollDown 2s ease-in-out infinite; }

  /* 导航 */
  .nav-link {
    position```css
  .nav-link {
    position: relative;
    padding-bottom: 4px;
    color: var(--cream);
    transition: color 0.3s ease;
  }
  .nav-link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 0;
    height: 1px;
    background: var(--gold);
    transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .nav-link:hover { color: var(--gold-light); }
  .nav-link:hover::after { width: 100%; }

  /* 数字标号 */
  .num-label {
    font-family: 'Cormorant Garamond', serif;
    color: var(--gold-dark);
    font-size: 12px;
    letter-spacing: 0.3em;
  }

  /* 瓶身光效 */
  .bottle-glow {
    filter: drop-shadow(0 0 40px rgba(201, 169, 97, 0.15)) drop-shadow(0 20px 30px rgba(0,0,0,0.6));
  }

  /* 旋转徽章 */
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .spin-slow { animation: spin 30s linear infinite; }

  /* 滚动条 */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 3px; }

  /* 选区 */
  ::selection { background: var(--gold); color: var(--bg); }
</style>
</head>
<body>

<!-- 导航 -->
<nav class="fixed top-0 left-0 right-0 z-50 backdrop-blur-md bg-black/40 border-b border-gold">
  <div class="max-w-7xl mx-auto px-6 lg:px-10 py-5 flex items-center justify-between">
    <div class="flex items-center gap-3">
      <svg width="28" height="28" viewBox="0 0 32 32" fill="none">
        <path d="M16 2 L28 10 L28 22 L16 30 L4 22 L4 10 Z" stroke="#c9a961" stroke-width="1"/>
        <path d="M16 8 L22 12 L22 20 L16 24 L10 20 L10 12 Z" stroke="#c9a961" stroke-width="1"/>
      </svg>
      <span class="font-serif text-xl tracking-[0.3em] text-cream">MAISON NOIR</span>
    </div>
    <div class="hidden md:flex items-center gap-10 text-xs tracking-[0.2em] uppercase">
      <a href="#" class="nav-link">藏酒</a>
      <a href="#" class="nav-link">产区</a>
      <a href="#" class="nav-link">品鉴</a>
      <a href="#" class="nav-link">私享会</a>
    </div>
    <div class="flex items-center gap-5">
      <button class="text-cream hover:text-gold-light transition">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.3-4.3"/></svg>
      </button>
      <button class="text-cream hover:text-gold-light transition relative">
</body>
</html>