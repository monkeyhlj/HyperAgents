<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON FLEUR — 花之邸 · 高定花艺</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600&family=Italiana&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0a0807;
    --velvet:#140d10;
    --velvet-2:#1d1216;
    --burgundy:#5a1a26;
    --burgundy-deep:#3a0f18;
    --rose:#c4566a;
    --rose-soft:#e8a8b3;
    --gold:#c9a55c;
    --gold-bright:#e6c987;
    --champagne:#f3e6d0;
    --paper:#f5ede0;
    --line:rgba(201,165,92,.22);
    --glass:rgba(245,237,224,.04);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    background:var(--velvet);
    color:var(--paper);
    font-family:'Noto Serif SC','Cormorant Garamond',serif;
    font-weight:300;
    line-height:1.6;
    overflow-x:hidden;
    -webkit-font-smoothing:antialiased;
  }
  ::selection{background:var(--gold);color:var(--ink)}

  /* ---------- grain & vignette ---------- */
  body::before{
    content:"";
    position:fixed;inset:0;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='200' height='200'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.9 0 0 0 0 0.85 0 0 0 0 0.8 0 0 0 0.08 0'/></filter><rect width='100%25' height='100%25' filter='url(%23n)'/></svg>");
    opacity:.5;mix-blend-mode:overlay;pointer-events:none;z-index:1000;
  }
  body::after{
    content:"";position:fixed;inset:0;
    background:radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,.55) 100%);
    pointer-events:none;z-index:999;
  }

  /* ---------- nav ---------- */
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:200;
    padding:22px 48px;
    display:flex;align-items:center;justify-content:space-between;
    transition:background .6s ease, backdrop-filter .6s ease, padding .4s ease;
  }
  .nav.scrolled{
    background:rgba(10,8,7,.78);
    backdrop-filter:blur(14px);
    padding:14px 48px;
    border-bottom:1px solid var(--line);
  }
  .nav .mark{
    font-family:'Italiana',serif;
    letter-spacing:.42em;
    font-size:15px;
    color:var(--gold-bright);
    text-transform:uppercase;
  }
  .nav .mark span{color:var(--rose-soft)}
  .nav ul{display:flex;gap:38px;list-style:none;
    font-family:'Cormorant Garamond',serif;
    font-size:13px;letter-spacing:.28em;text-transform:uppercase;
  }
  .nav ul a{
    color:var(--champagne);text-decoration:none;opacity:.72;
    transition:opacity .4s ease,color .4s ease;
    position:relative;
  }
  .nav ul a::after{
    content:"";position:absolute;left:0;bottom:-6px;height:1px;width:0;
    background:var(--gold);transition:width .5s ease;
  }
  .nav ul a:hover{opacity:1;color:var(--gold-bright)}
  .nav ul a:hover::after{width:100%}
  .nav .cta-mini{
    font-family:'Cormorant Garamond',serif;
    font-size:12px;letter-spacing:.3em;text-transform:uppercase;
    color:var(--ink);background:linear-gradient(120deg,var(--gold-bright),var(--gold));
    padding:10px 22px;border-radius:2px;text-decoration:none;
    transition:transform .4s ease,box-shadow .4s ease;
  }
  .nav .cta-mini:hover{transform:translateY(-2px);box-shadow:0 8px 24px rgba(201,165,92,.3)}

  /* ---------- HERO ---------- */
  .hero{
    position:relative;min-height:100vh;width:100%;
    display:grid;grid-template-columns:1.1fr .9fr;
    align-items:center;
    padding:120px 64px 80px;
    overflow:hidden;
    background:
      radial-gradient(circle at 78% 30%, rgba(90,26,38,.55), transparent 55%),
      radial-gradient(circle at 20% 80%, rgba(20,13,16,.9), transparent 60%),
      linear-gradient(160deg,#1a0f13 0%, #0f0809 60%, #0a0606 100%);
  }
  .hero::before{
    content:"";position:absolute;inset:0;
    background-image:
      linear-gradient(transparent 0, transparent calc(100% - 1px), var(--line) 100%),
      linear-gradient(90deg, transparent 0, transparent calc(100% - 1px), var(--line) 100%);
    background-size:80px 80px;
    opacity:.15;pointer-events:none;
  }

  .hero-left{position:relative;z-index:5;max-width:680px}
  .eyebrow{
    display:inline-flex;align-items:center;gap:14px;
    font-family:'Cormorant Garamond',serif;
    font-size:12px;letter-spacing:.5em;text-transform:uppercase;
    color:var(--gold);margin-bottom:36px;
  }
  .eyebrow::before,.eyebrow::after{
    content:"";display:block;width:42px;height:1px;background:var(--gold);opacity:.6;
  }
  .hero h1{
    font-family:'Italiana',serif;
    font-size:clamp(56px,8.4vw,128px);
    line-height:.92;font-weight:400;
    color:var(--champagne);
    letter-spacing:-.01em;
    margin-bottom:8px;
  }
  .hero h1 .it{
    font-family:'Cormorant Garamond',serif;font-style:italic;
    font-weight:300;color:var(--rose-soft);
    display:block;font-size:clamp(38px,5.6vw,86px);
    margin-top:6px;
  }
  .hero h1 .cn{
    font-family:'Noto Serif SC',serif;font-weight:300;
    font-size:clamp(18px,2vw,28px);letter-spacing:.5em;
    color:var(--gold-bright);display:block;margin-top:28px;
    padding-left:.5em;
  }
  .hero .lede{
    max-width:460px;margin-top:42px;
    font-size:16px;line```
-height:1.9;color:rgba(245,237,224,.78);
    font-family:'Noto Serif SC',serif;font-weight:300;
  }
  .hero .lede em{color:var(--rose-soft);font-style:normal;letter-spacing:.04em}
  .hero-actions{display:flex;gap:24px;margin-top:48px;align-items:center}
  .btn-primary{
    font-family:'Cormorant Garamond',serif;font-size:13px;letter-spacing:.34em;text-transform:uppercase;
    color:var(--ink);background:linear-gradient(120deg,var(--gold-bright),var(--gold));
    padding:16px 34px;border-radius:2px;text-decoration:none;
    transition:transform .4s ease,box-shadow .4s ease;position:relative;overflow:hidden;
  }
  .btn-primary::before{
    content:"";position:absolute;inset:0;
    background:linear-gradient(120deg,transparent,rgba(255,255,255,.4),transparent);
    transform:translateX(-100%);transition:transform .8s ease;
  }
  .btn-primary:hover{transform:translateY(-2px);box-shadow:0 12px 32px rgba(201,165,92,.35)}
  .btn-primary:hover::before{transform:translateX(100%)}
  .btn-ghost{
    font-family:'Cormorant Garamond',serif;font-size:13px;letter-spacing:.34em;text-transform:uppercase;
    color:var(--champagne);text-decoration:none;
    padding:16px 0;border-bottom:1px solid var(--line);
    transition:color .4s ease,border-color .4s ease;
  }
  .btn-ghost:hover{color:var(--gold-bright);border-color:var(--gold)}

  /* signature ring */
  .hero-right{position:relative;z-index:5;height:78vh;display:flex;align-items:center;justify-content:center}
  .ring{
    position:relative;width:min(560px,46vw);aspect-ratio:1;
    display:flex;align-items:center;justify-content:center;
  }
  .ring svg{position:absolute;inset:0;width:100%;height:100%}
  .ring .ring-outer{animation:spin 80s linear infinite}
  .ring .ring-mid{animation:spin 60s linear infinite reverse}
  .ring .ring-inner{animation:spin 40s linear infinite}
  @keyframes spin{to{transform:rotate(360deg)}}
  .ring text{
    font-family:'Cormorant Garamond',serif;font-size:11px;letter-spacing:.4em;
    fill:var(--gold);text-transform:uppercase;
  }
  .ring text.amber{fill:var(--rose-soft)}
  .ring .core{
    position:relative;z-index:3;width:62%;aspect-ratio:1;border-radius:50%;
    background:
      radial-gradient(circle at 50% 40%, rgba(230,201,135,.18), transparent 60%),
      radial-gradient(circle at 50% 70%, rgba(90,26,38,.6), transparent 70%),
      linear-gradient(180deg, rgba(20,13,16,.6), rgba(10,8,7,.85));
    border:1px solid var(--line);
    box-shadow:inset 0 0 60px rgba(0,0,0,.6), 0 30px 80px rgba(0,0,0,.5);
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    text-align:center;padding:8%;
  }
  .ring .core .glyph{
    font-family:'Italiana',serif;font-size:clamp(40px,5vw,72px);
    color:var(--gold-bright);line-height:1;letter-spacing:.02em;
  }
  .ring .core .glyph .petal{color:var(--rose-soft);font-style:italic;font-family:'Cormorant Garamond',serif}
}
</style>
</html>
```