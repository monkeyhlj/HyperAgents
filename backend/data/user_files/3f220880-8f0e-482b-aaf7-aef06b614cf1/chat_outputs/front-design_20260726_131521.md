我来为你设计一个高端大气的酒类网站首页。设计灵感来自顶级酒庄的奢华质感，采用深色基调配金色点缀，营造沉浸式的视觉体验。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR · 臻藏佳酿</title>
<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
  :root {
    --gold: #c9a96a;
    --gold-light: #e8d5a8;
    --gold-dark: #8b7340;
    --bg-dark: #0a0a0a;
    --bg-card: #141414;
    --text-light: #f5f0e8;
    --text-muted: #8a8580;
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    font-family: 'Noto Serif SC', serif;
    background: var(--bg-dark);
    color: var(--text-light);
    overflow-x: hidden;
  }
  .font-display { font-family: 'Cormorant Garamond', serif; }
  .text-gold { color: var(--gold); }
  .bg-gold { background: var(--gold); }
  .border-gold { border-color: var(--gold); }
  
  /* Hero背景 */
  .hero-bg {
    background: linear-gradient(135deg, rgba(10,10,10,0.7) 0%, rgba(10,10,10,0.4) 50%, rgba(10,10,10,0.85) 100%),
                url('https://images.unsplash.com/photo-1510812431401-41d2fd4d9d7a?w=1920&q=80') center/cover;
    min-height: 100vh;
  }
  .hero-bg::after {
    content: '';
    position: absolute; inset: 0;
    background: radial-gradient(ellipse at 70% 50%, rgba(201,169,106,0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  /* 金色渐变文字 */
  .gold-text {
    background: linear-gradient(135deg, #e8d5a8 0%, #c9a96a 50%, #8b7340 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* 玻璃质感导航 */
  .nav-glass {
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(201,169,106,0.15);
  }

  /* 装饰线 */
  .divider-gold {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  /* 产品卡片 */
  .product-card {
    background: linear-gradient(180deg, #1a1a1a 0%, #0f0f0f 100%);
    border: 1px solid rgba(201,169,106,0.1);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .product-card:hover {
    border-color: rgba(201,169,106,0.4);
    transform: translateY(-8px);
    box-shadow: 0 20px 60px rgba(0,0,0,0.5), 0 0 40px rgba(201,169,106,0.08);
  }
  .product-card img { transition: transform 0.7s ease; }
  .product-card:hover img { transform: scale(1.08); }

  /* 按钮 */
  .btn-gold {
    background: linear-gradient(135deg, #c9a96a 0%, #8b7340 100%);
    color: #0a0a0a;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  .btn-gold::before {
    content: '';
    position: absolute; top: 0; left: -100%;
    width: 100%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.6s ease;
  }
  .btn-gold:hover::before { left: 100%; }
  .btn-gold:hover { box-shadow: 0 8px 30px rgba(201,169,106,0.3); }

  .btn-outline {
    border: 1px solid var(--gold);
    color: var(--gold);
    transition: all 0.3s ease;
  }
  .btn-outline:hover {
    background: var(--gold);
    color:#0a0a0a;
  }

  /* 滚动揭示动画 */
  .reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }

  /* 漂浮装饰 */
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(3deg); }
  }
  .float-anim { animation: float 6s ease-in-out infinite; }

  /* 酒滴动画 */
  @keyframes drop {
    0% { transform: translateY(-100vh) scale(0.8); opacity: 0; }
    10% { opacity: 0.6; }
    90% { opacity: 0.6; }
    100% { transform: translateY(100vh) scale(1.2); opacity: 0; }
  }
  .wine-drop {
    position: fixed;
    width: 4px; height: 12px;
    background: linear-gradient(180deg, transparent, var(--gold));
    border-radius: 50%;
    pointer-events: none;
    z-index: 1;
  }

  /* 品鉴笔记 */
  .taste-note {
    border-left: 2px solid var(--gold);
    padding-left: 1.5rem;
  }

  /* 数字滚动 */
  .stat-number {
    font-variant-numeric: tabular-nums;
    letter-spacing: -0.02em;
  }

  /* 滚动条 */
  ::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: #0a0a0a; }
  ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--gold); }

  /* 磨砂卡片 */
  .frosted {
    background: rgba(20,20,20,0.6);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(201,169,106,0.12);
  }

  /* 标签 */
  .tag-gold {
    display: inline-block;
    padding: 4px 14px;
    border: 1px solid rgba(201,169,106,0.4);
    border-radius: 999px;
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    color: var(--gold-light);
    text-transform: uppercase;
  }

  /* 旋转徽章 */
  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .spin-slow { animation: spin-slow 20s linear infinite; }

  /* 渐入光晕 */
  .glow-spot {
    position: absolute;
    width: 500px; height: 500px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(201,169,106,0.06) 0%, transparent 70%);
    pointer-events: none;
  }
</style>
</head>
<body>

<!-- 导航栏 -->
<nav class="nav-glass fixed top-0 left-0 right-0 z-50 px-6 lg:px-12 py-4">
  <div class="flex items-center justify-between max-w-7xl mx-auto">
    <div class="flex items-center gap-3">
      <div class="w-10 h-10 border border-gold rounded-full flex items-center justify-center">
        <span class="font-display text-gold text-xl font-semibold">M</span>
      </div>
      <div>
        <h1 class="font-display text-xl tracking-[0.3em] text-gold">MAISON NOIR</h1>
        <p class="text-[0.6rem] tracking-[0.4em] text-gray-500 -mt-1">EST · 1892</p>
      </div>
    </div>
    <ul class="hidden lg:flex items-center gap-10 text-sm tracking-widest">
      <li><a href="#" class="text-gold border-b border-gold pb-1">首页</a></li>
      <li><a href="#collection" class="hover:text-gold transition">臻藏系列</a></li>
      <li><a href="#story" class="hover:text-gold transition">品牌故事</a></li>
      <li><a href="#craft" class="hover:text-gold transition">酿造工艺</a></li>
      <li><a href="#tasting" class="hover:text-gold transition">品鉴预约</a></li>
      <li><a href="#contact" class="hover:text-gold transition">联系我们</a></li>
    </ul>
    <div class="flex items-center gap-4">
      <button class="text-gray-400 hover:text-gold transition"><i class="fas fa-search"></i></button>
      <button class="text-gray-400 hover:text-gold transition"><i class="fas fa-user"></i></button>
      <button class="relative text-gray-400 hover:text-gold transition">
        <i class="fas fa-shopping-bag"></i>
        <span class="absolute -top-2 -right-2 w-4 h-4 bg-gold text-black text-[0.6rem] rounded-full flex items-center justify-center">2</span>
      </button>
    </div>
  </div>
</nav>

<!-- Hero 区域 -->
<section class="hero-bg relative flex items-center justify-center px-6">
  <div class="text-center max-w-4xl relative z-10">
    <div class="mb-6 flex justify-center">
      <span class="tag-gold">Grand Cru · 限量臻藏</span>
    </div>
    <p class="font-display text-gold-light text-lg tracking-[0.4em] mb-4">— 时光酿造的艺术 —</p>
    <h2 class="font-display text-6xl md:text-8xl font-light leading-tight mb-6">
      <span class="gold-text">岁月之味</span><br>
      <span class="text-white italic font-extralight">The Soul of Vintage</span>
    </h2>
    <p class="text-gray-400 text-base md:text-lg max-w-2xl mx-auto leading-relaxed mb-10">
      自1892年传承至今，每一滴佳酿皆源自百年葡萄园的晨露与暮光，<br class="hidden md:block">
      于橡木桶中沉睡，于杯盏中苏醒。
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <button class="btn-gold px-10 py-4 rounded-full font-medium tracking-widest text-sm">探索臻藏 →</button>
      <button class="btn-outline px-10 py-4 rounded-full font-medium tracking-widest text-sm">预约品鉴</button>
    </div>
  </div>

  <!-- 滚动提示 -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2">
    <span class="text-[0.6rem] tracking-[0.4em] text-gray-500">SCROLL</span>
    <div class="w-px h-12 bg-gradient-to-b from-transparent via-gold to-transparent"></div>
  </div>

  <!-- 旋转徽章 -->
  <div class="absolute top-1/2 right-12 -translate-y-1/2 hidden lg:block">
    <div class="w-32 h-32 relative">
      <svg class="spin-slow w-full h-full" viewBox="0 0 100 100">
        <defs>
          <path id="circle" d="M 50,50 m -38,0 a 38,38 0 1,1 76,0 a 38,38 0 1,1 -76,0"/>
        </defs>
        <text class="fill-[#c9a96a]" style="font-size:7px;letter-spacing:3px;font-family:'Cormorant Garamond'">
          <textPath href="#circle">MAISON NOIR · EST 1892 · GRAND CRU · </textPath>
        </text>
      </svg>
      <div class="absolute inset-0 flex items-center justify-center">
        <i class="fas fa-wine-glass-alt text-gold text-2xl"></i>
      </div>
    </div>
  </div>
</section>

<!-- 品牌数据 -->
<section class="py-20 px-6 lg:px-12 border-y border-gray-900 relative">
  <div class="glow-spot" style="top:-100px;left:50%;transform:translateX(-50%)

使用的 Skill: front-design