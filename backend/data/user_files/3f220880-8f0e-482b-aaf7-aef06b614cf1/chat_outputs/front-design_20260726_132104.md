我来为你设计一个高端大气的卖酒网站首页。我会使用深色奢华配色、优雅的衬线字体和精致的视觉细节来营造高端感。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR · 臻藏佳酿</title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@300;400;500;600;700&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0807;
    --bg-2: #14100c;
    --gold: #c9a961;
    --gold-light: #e8d4a0;
    --gold-dark: #8a7344;
    --cream: #f5efe1;
    --muted: #8a7e6a;
    --border: rgba(201, 169, 97, 0.2);
  }
  * { -webkit-font-smoothing: antialiased; }
  body {
    background: var(--bg);
    color: var(--cream);
    font-family: 'Jost', 'Noto Serif SC', sans-serif;
    overflow-x: hidden;
  }
  .font-serif { font-family: 'Cormorant Garamond', 'Noto Serif SC', serif; }
  .font-cn { font-family: 'Noto Serif SC', serif; }

  /* 金色渐变文字 */
  .gold-text {
    background: linear-gradient(135deg, #e8d4a0 0%, #c9a961 40%, #8a7344 70%, #c9a961 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .gold-border {
    border-color: var(--border);
  }

  /* 噪点纹理 */
  .noise::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.4'/%3E%3C/svg%3E");
    opacity: 0.06;
    pointer-events: none;
    mix-blend-mode: overlay;
  }

  /* 光晕 */
  .glow {
    background: radial-gradient(circle at center, rgba(201,169,97,0.18) 0%, transparent 60%);
  }

  /* 瓶身光泽动画 */
  @keyframes shimmer {
    0%, 100% { opacity: 0.3; transform: translateX(-20%); }
    50% { opacity: 0.6; transform: translateX(20%); }
  }
  .shimmer { animation: shimmer 6s ease-in-out infinite; }

  /* 缓慢浮动 */
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-12px) rotate(0.5deg); }
  }
  .float { animation: float 8s ease-in-out infinite; }

  /* 滚动渐入 */
  .reveal { opacity: 0; transform:translateY(40px); transition: opacity 1.2s cubic-bezier(0.16,1,0.3,1), transform 1.2s cubic-bezier(0.16,1,0.3,1); }
  .reveal.in { opacity: 1; transform: translateY(0); }

  /* 分隔线 */
  .divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  /* 按钮 */
  .btn-gold {
    background: linear-gradient(135deg, #e8d4a0, #c9a961);
    color: #0a0807;
    transition: all 0.4s ease;
  }
  .btn-gold:hover {
    box-shadow: 0 0 30px rgba(201,169,97,0.4);
    transform: translateY(-2px);
  }
  .btn-ghost {
    border: 1px solid var(--border);
    color: var(--cream);
    transition: all 0.4s ease;
  }
  .btn-ghost:hover {
    border-color: var(--gold);
    background: rgba(201,169,97,0.05);
  }

  /* 酒瓶 CSS 绘制 */
  .bottle {
    position: relative;
    width: 120px;
    height: 380px;
    margin: 0 auto;
  }
  .bottle-neck {
    position: absolute;
    top: 0; left: 50%;
    transform: translateX(-50%);
    width: 32px; height: 70px;
    background: linear-gradient(180deg, #1a1410 0%, #2a1f15 100%);
    border-radius: 4px 4px 0 0;
    box-shadow: inset -3px 0 6px rgba(0,0,0,0.5), inset 3px 0 6px rgba(201,169,97,0.1);
  }
  .bottle-cap {
    position: absolute;
    top: -8px; left: 50%;
    transform: translateX(-50%);
    width: 36px; height: 18px;
    background: linear-gradient(135deg, #c9a961, #8a7344);
    border-radius: 3px;
  }
  .bottle-body {
    position: absolute;
    bottom: 0; left: 50%;
    transform: translateX(-50%);
    width: 120px; height: 320px;
    background: linear-gradient(135deg, #1a1410 0%, #3a2a1c 30%, #2a1f15 60%, #1a1410 100%);
    border-radius: 14px 14px 18px 18px;
    box-shadow: inset -8px 0 20px rgba(0,0,0,0.6), inset 8px 0 20px rgba(201,169,97,0.08), 0 20px 60px rgba(0,0,0,0.5);
    overflow: hidden;
  }
  .bottle-label {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%,-50%);
    width: 88px; height: 140px;
    background: linear-gradient(180deg, #f5efe1 0%, #e8dcc4 100%);
    border: 1px solid var(--gold);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 8px;
    text-align: center;
  }
  .bottle-shine {
    position: absolute;
    top: 0; left: 20%;
    width: 12px; height: 100%;
    background: linear-gradient(180deg, transparent, rgba(255,255,255,0.15), transparent);
    filter: blur(4px);
  }

  /* 滚动条 */
 ```css
::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--gold); }

  /* 导航栏 */
  .nav-link { position: relative; transition: color 0.3s ease; }
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -4px; left: 0;
    width: 0; height: 1px;
    background: var(--gold);
    transition: width 0.4s ease;
  }
  .nav-link:hover { color: var(--gold-light); }
  .nav-link:hover::after { width: 100%; }

  /* 产品卡 */
  .product-card {
    background: linear-gradient(180deg, rgba(20,16,12,0.6), rgba(10,8,7,0.9));
    border: 1px solid var(--border);
    transition: all 0.6s cubic-bezier(0.16,1,0.3,1);
  }
  .product-card:hover {
    border-color: rgba(201,169,97,0.5);
    transform: translateY(-8px);
    box-shadow: 0 30px 80px rgba(0,0,0,0.6), 0 0 40px rgba(201,169,97,0.1);
  }

  /* 标签徽章 */
  .badge {
    background: linear-gradient(135deg, rgba(201,169,97,0.15), rgba(201,169,97,0.05));
    border: 1px solid var(--border);
    color: var(--gold-light);
  }

  /* 大标题字间距 */
  .tracking-luxe { letter-spacing: 0.3em; }
  .tracking-wide-2 { letter-spacing: 0.15em; }

  /* 背景装饰圆 */
  .deco-circle {
    position: absolute;
    border: 1px solid var(--border);
    border-radius: 50%;
    pointer-events: none;
  }

  /* 旋转环 */
  @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
  .rotate-slow { animation: rotate 40s linear infinite; }

  /* 数字标号 */
  .num-label {
    font-family: 'Cormorant Garamond', serif;
    color: var(--gold);
    font-size: 0.75rem;
    letter-spacing: 0.3em;
  }
</style>
</head>
<body class="relative">

<!-- 顶部公告条 -->
<div class="relative z-50 bg-[#14100c] border-b gold-border text-center py-2 text-xs tracking-wide-2 text-[#8a7e6a]">
  <span class="text-[var(--gold)]">✦</span> 全场顺丰冷链配送 · 单笔满 ¥2,888 赠限量手工木盒 <span class="text-[var(--gold)]">✦</span>
</div>

<!-- 导航栏 -->
<header class="relative z-40 px-6 lg:px-16 py-6 flex items-center justify-between border-b gold-border">
  <div class="flex items-center gap-3">
    <div class="w-10 h-10 border gold-border flex items-center justify-center rotate-45">
      <div class="w-4 h-4 bg-[var(--gold)] -rotate-45"></div>
    </div>
    <div>
      <div class="font-serif text-xl tracking-luxe gold-text font-semibold">MAISON NOIR</div>
      <div class="text-[10px] tracking-wide-2 text-[var(--muted)] -mt-1">臻藏佳酿 · EST. 1928</div>
    </div>
  </div>
</body>
</html>

使用的 Skill: front-design