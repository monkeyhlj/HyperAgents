<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>花庐 HUALU — 一日一花，一花一季</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600;700&family=JetBrains+Mono:wght@300;400&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#1a1612;
    --paper:#f5f0e6;
    --paper-warm:#ede4d3;
    --moss:#3d4a2a;
    --moss-deep:#2a3320;
    --rouge:#7a2e2e;
    --gold:#a8895c;
    --gold-soft:#c4a878;
    --shadow:rgba(26,22,18,0.08);
  }

  *{margin:0;padding:0;box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    background:var(--paper);
    color:var(--ink);
    font-family:'Noto Serif SC',serif;
    font-weight:300;
    line-height:1.7;
    overflow-x:hidden;
    -webkit-font-smoothing:antialiased;
  }

  /* ============ NAV ============ */
  nav{
    position:fixed;top:0;left:0;right:0;
    z-index:100;
    padding:1.4rem 3rem;
    display:flex;justify-content:space-between;align-items:center;
    background:linear-gradient(to bottom,rgba(245,240,230,0.92),rgba(245,240,230,0));
    backdrop-filter:blur(2px);
    transition:padding .4s ease;
  }
  nav.scrolled{padding:.9rem 3rem;background:rgba(245,240,230,0.96);border-bottom:1px solid rgba(168,137,92,0.18)}
  .logo{
    font-family:'Cormorant Garamond',serif;
    font-size:1.5rem;font-weight:500;
    letter-spacing:.18em;
    color:var(--ink);
  }
  .logo span{color:var(--rouge);font-style:italic;font-weight:400}
  .nav-links{display:flex;gap:2.5rem;list-style:none}
  .nav-links a{
    color:var(--ink);text-decoration:none;
    font-size:.78rem;letter-spacing:.22em;
    text-transform:uppercase;
    font-family:'JetBrains Mono',monospace;
    font-weight:400;
    position:relative;padding-bottom:2px;
    transition:color .3s;
  }
  .nav-links a::after{
    content:'';position:absolute;bottom:0;left:0;
    width:0;height:1px;background:var(--rouge);
    transition:width .4s ease;
  }
  .nav-links a:hover{color:var(--rouge)}
  .nav-links a:hover::after{width:100%}
  .nav-cta{
    font-family:'JetBrains Mono',monospace;
    font-size:.72rem;letter-spacing:.2em;
    color:var(--paper);background:var(--ink);
    padding:.65rem 1.4rem;
    text-decoration:none;
    text-transform:uppercase;
    transition:background .3s;
  }
  .nav-cta:hover{background:var(--rouge)}

  /* ============ HERO ============ */
  .hero{
    height:100vh;min-height:780px;
    position:relative;
    display:grid;
    grid-template-columns:1.1fr 1fr;
    overflow:hidden;
  }
  .hero-left{
    padding:8rem 4rem 3rem 5rem;
    display:flex;flex-direction:column;justify-content:center;
    position:relative;
    z-index:2;
  }
  .hero-eyebrow{
    font-family:'JetBrains Mono',monospace;
    font-size:.72rem;letter-spacing:.4em;
    color:var(--moss);
    text-transform:uppercase;
    margin-bottom:2.5rem;
    display:flex;align-items:center;gap:1rem;
  }
  .hero-eyebrow::before{
    content:'';width:3rem;height:1px;background:var(--moss);
  }
  .hero-title{
    font-family:'Cormorant Garamond',serif;
    font-size:clamp(3.5rem,7vw,6.8rem);
    font-weight:300;
    line-height:.95;
    color:var(--ink);
    letter-spacing:-.02em;
    margin-bottom:1.5rem;
  }
  .hero-title .cn{
    display:block;
    font-family:'Noto Serif SC',serif;
    font-weight:300;
    font-size:clamp(2rem,3.5vw,3.2rem);
    letter-spacing:.15em;
    color:var(--moss-deep);
    margin-top:.6rem;
  }
  .hero-title em{
    font-style:italic;
    color:var(--rouge);
    font-weight:400;
  }
  .hero-sub{
    font-size:.95rem;
    max-width:28rem;
    color:rgba(26,22,18,0.72);
    margin:2rem 0 3rem;
    line-height:1.9;
    font-weight:300;
  }
  .hero-actions{display:flex;gap:1.2rem;align-items:center}
  .btn-primary{
    background:var(--ink);color:var(--paper);
    padding:1.1rem 2.2rem;
    text-decoration:none;
    font-family:'JetBrains Mono',monospace;
    font-size:.75rem;letter-spacing:.25em;
    text-transform:uppercase;
    transition:all .4s;
    display:inline-flex;align-items:center;gap:.8rem;
  }
  .btn-primary:hover{background:var(--rouge);padding-right:2.8rem}
  .btn-primary .arrow{transition:transform .4s}
  .btn-primary:hover .arrow{transform:translateX(.4rem)}
  .btn-ghost{
    color:var(--ink);
    text-decoration:none;
    font-family:'JetBrains Mono',monospace;
    font-size:.75rem;letter-spacing:.25em;
    text-transform:uppercase;
    border-bottom:1px solid var(--ink);
    padding-bottom:.3rem;
    transition:color .3s,border-color .3s;
  }
  .btn-ghost:hover{color:var(--rouge);border-color:var(--rouge)}

  .hero-right{
    position:relative;
    overflow:hidden;
    background:var(--paper-warm);
  }
  .hero-image{
    position:absolute;inset:0;
    background-image:url('https://images.unsplash.com/photo-1561181286-d3fee1c9b1a9?w=1400&q=80');
    background-size:cover;background-position:center;
    filter:saturate(.85) contrast(1.05);
  }
  .hero-image::after{
    content:'';position:absolute;inset:0;
    background:linear-gradient(135deg,rgba(245,240,230,0.4) 0%,rgba(245,240,230,0) 30%,rgba(26,22,18,0.15) 100%);
  }
  .hero-meta{
    position:absolute;
    bottom:3rem;right:3rem;
    z-index:3;
    text-align:right;
    color:var(--paper);
  }
  .hero-meta .num{
    font-family:'Cormorant Garamond',serif;
    font-size:5rem;font-weight:300;
   ```html
line-height:1;
    color:var(--paper);
    text-shadow:0 2px 20px rgba(0,0,0,0.3);
  }
  .hero-meta .label{
    font-family:'JetBrains Mono',monospace;
    font-size:.68rem;letter-spacing:.3em;
    text-transform:uppercase;
    margin-top:.4rem;
    opacity:.85;
  }
  .hero-vertical{
    position:absolute;
    top:50%;left:2rem;
    transform:translateY(-50%) rotate(-90deg);
    transform-origin:left center;
    font-family:'JetBrains Mono',monospace;
    font-size:.65rem;letter-spacing:.5em;
    color:var(--paper);
    text-transform:uppercase;
    opacity:.7;
    z-index:3;
    white-space:nowrap;
  }
  .hero-scroll{
    position:absolute;
    bottom:2.5rem;left:5rem;
    font-family:'JetBrains Mono',monospace;
    font-size:.65rem;letter-spacing:.3em;
    color:var(--ink);
    text-transform:uppercase;
    display:flex;align-items:center;gap:.8rem;
    z-index:3;
  }
  .hero-scroll::after{
    content:'';width:1px;height:3rem;
    background:var(--ink);
    animation:scrollLine 2s ease-in-out infinite;
    transform-origin:top;
  }
  @keyframes scrollLine{
    0%,100%{transform:scaleY(.3)}
    50%{transform:scaleY(1)}
  }

  /* ============ SECTION COMMON ============ */
  .section-label{
    font-family:'JetBrains Mono',monospace;
    font-size:.7rem;letter-spacing:.4em;
    color:var(--moss);
    text-transform:uppercase;
    display:flex;align-items:center;gap:1rem;
    margin-bottom:2rem;
  }
  .section-label::before{
    content:'';width:2.5rem;height:1px;background:var(--moss);
  }
  .section-title{
    font-family:'Cormorant Garamond',serif;
    font-size:clamp(2.5rem,4.5vw,4rem);
    font-weight:300;
    line-height:1.05;
    letter-spacing:-.01em;
    color:var(--ink);
    margin-bottom:1.5rem;
  }
  .section-title em{font-style:italic;color:var(--rouge);font-weight:400}
  .section-title .cn{
    display:block;
    font-family:'Noto Serif SC',serif;
    font-weight:300;
    font-size:clamp(1.4rem,2vw,1.8rem);
    letter-spacing:.12em;
    color:var(--moss-deep);
    margin-top:.5rem;
  }

  /* ============ FEATURED COLLECTION ============ */
  .featured{
    padding:8rem 5rem;
    background:var(--paper);
    position:relative;
  }
  .featured-header{
    display:flex;justify-content:space-between;align-items:flex-end;
    margin-bottom:5rem;
    border-bottom:1px solid rgba(168,137,92,0.25);
    padding-bottom:2rem;
  }
  .featured-header p{
    max-width:22rem;
    font-size:.88rem;
    color:rgba(26,22,18,0.6);
    line-height:1.8;
  }
  .collection-grid{
    display:grid;
    grid-template-columns:repeat(3,1fr);
    gap:2.5rem;
  }
  .flower-card{
    position:relative;
    cursor:pointer;
  }
  .flower-card-img{
    aspect-ratio:3/4;
    overflow:hidden;
    background:var(--paper-warm);
    margin-bottom:1.5rem;
    position:relative;
  }
  .flower-card-img img{
    width:100%;height:100%;
    object-fit:cover;
    transition:transform 1.2s cubic-bezier(.16,1,.3,1),filter .6s;
    filter:saturate(.9);
  }
  .flower-card:hover .flower-card-img img{
    transform:scale(1.06);
    filter:saturate(1);
  }
  .flower-card-img::after{
    content:'';position:absolute;inset:0;
    background:linear-gradient(to top,rgba(26,22,18,0.2),transparent 50%);
    opacity:0;transition:opacity .5s;
  }
  .flower-card:hover .flower-card-img::after{opacity:1}
  .flower-card-tag{
    position:absolute;top:1rem;left:1rem;
    font-family:'JetBrains Mono',monospace;
    font-size:.6rem;letter-spacing:.25em;
    color:var(--paper);
    background:rgba(26,22,18,0.7);
    padding:.4rem .7rem;
    text-transform:uppercase;
    z-index:2;
  }
  .flower-card-name{
    font-family:'Cormorant Garamond',serif;
    font-size:1.6rem;font-weight:400;
    font-style:italic;
    color:var(--ink);
    margin-bottom:.3rem;
  }
  .flower-card-cn{
    font-family:'Noto Serif SC',serif;
    font-size:.85rem;
    color:var(--moss);
    letter-spacing:.1em;
    margin-bottom```html
:1rem;
  }
  .flower-card-desc{
    font-size:.8rem;
    color:rgba(26,22,18,0.55);
    line-height:1.7;
    margin-bottom:1rem;
  }
  .flower-card-price{
    font-family:'JetBrains Mono',monospace;
    font-size:.82rem;
    color:var(--ink);
    letter-spacing:.05em;
    display:flex;justify-content:space-between;align-items:center;
    padding-top:1rem;
    border-top:1px solid rgba(168,137,92,0.2);
  }
  .flower-card-price .currency{color:var(--rouge)}

  /* ============ BRAND STORY ============ */
  .story{
    padding:9rem 5rem;
    background:var(--moss-deep);
    color:var(--paper);
    position:relative;
    overflow:hidden;
  }
  .story::before{
    content:'花';
    position:absolute;
    font-family:'Noto Serif SC',serif;
    font-size:42rem;
    font-weight:700;
    color:rgba(245,240,230,0.025);
    top:50%;left:50%;
    transform:translate(-50%,-50%);
    line-height:1;
    pointer-events:none;
  }
  .story-grid{
    display:grid;
    grid-template-columns:1fr 1.3fr;
    gap:6rem;
    align-items:center;
    position:relative;
    z-index:2;
  }
  .story-text .section-label{color:var(--gold-soft)}
  .story-text .section-label::before{background:var(--gold-soft)}
  .story-text .section-title{color:var(--paper)}
  .story-text .section-title em{color:var(--gold-soft)}
  .story-text .section-title .cn{color:rgba(245,240,230,0.7)}
  .story-body{
    font-size:.95rem;
    line-height:2;
    color:rgba(245,240,230,0.75);
    margin-bottom:2rem;
  }
  .story-body p{margin-bottom:1.5rem}
  .story-body .lead{
    font-family:'Cormorant Garamond',serif;
    font-size:1.4rem;
    font-style:italic;
    color:var(--gold-soft);
    line-height:1.6;
    margin-bottom:2rem;
    border-left:2px solid var(--gold);
    padding-left:1.5rem;
  }
  .story-signature{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;
    font-size:1.1rem;
    color:var(--gold-soft);
    margin-top:2rem;
  }
  .story-signature::before{
    content:'— ';
    color:var(--gold);
  }
  .story-visual{
    position:relative;
    aspect-ratio:4/5;
  }
  .story-visual img{
    width:100%;height:100%;
    object-fit:cover;
    filter:saturate(.8) contrast(1.1) brightness(.95);
  }
  .story-visual::before{
    content:'';
    position:absolute;
    top:-1.5rem;left:-1.5rem;
    width:30%;height:30%;
    border-top:1px solid var(--gold);
    border-left:1px solid var(--gold);
  }
  .story-visual::after{
    content:'';
    position:absolute;
    bottom:-1.5rem;right:-1.5rem;
    width:30%;height:30%;
    border-bottom:1px solid var(--gold);
    border-right:1px solid var(--gold);
  }
  .story-caption{
    position:absolute;
    bottom:1.5rem;left:1.5rem;
    font-family:'JetBrains Mono',monospace;
    font-size:.65rem;letter-spacing:.25em;
    color:var(--paper);
    text-transform:uppercase;
    background:rgba(42,51,32,0.85);
    padding:.5rem .8rem;
  }

  /* ============ EXPERIENCE / SERVICE ============ */
  .experience{
    padding:8rem 5rem;
    background:var(--paper);
  }
  .experience-header{
    text-align:center;
    max-width:42rem;
    margin:0 auto 5rem;
  }
  .experience-header .section-label{
    justify-content:center;
  }
  .experience-header .section-label::after{
    content:'';width:2.5rem;height:1px;background:var(--moss);
  }
  .experience-grid{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:0;
    border-top:1px solid rgba(168,137,92,0.3);
    border-bottom:1px solid rgba(168,137,92,0.3);
  }
  .exp-item{
    padding:3rem 2rem;
    border-right:1px solid rgba(168,137,92,0.2);
    text-align:center;
    transition:background .5s;
  }
  .exp-item:last-child{border-right:none}
  .exp-item:hover{background:var(--paper-warm)}
  .exp-icon{
    font-family:'Cormorant Garamond',serif;
    font-size:2.8rem;
    font-style:italic;
}
</style>
</html>