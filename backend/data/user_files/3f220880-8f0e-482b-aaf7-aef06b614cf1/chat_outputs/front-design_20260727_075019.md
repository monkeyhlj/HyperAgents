<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON FLEUR — 高定花艺 · 自1924</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600&family=Italiana&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0a0807;
    --velvet:#140d0c;
    --velvet-2:#1d1311;
    --burgundy:#5a1a1f;
    --burgundy-deep:#3a0f13;
    --gold:#c9a96a;
    --gold-bright:#e8c98a;
    --gold-dim:#8a7344;
    --amber:#d98a3d;
    --cream:#f3ead8;
    --rose:#8a3a4a;
    --line:rgba(201,169,106,.22);
    --glass:rgba(243,234,216,.04);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html,body{background:var(--ink);color:var(--cream);font-family:'Noto Serif SC',serif;-webkit-font-smoothing:antialiased;overflow-x:hidden}
  body{line-height:1.6}
  ::selection{background:var(--gold);color:var(--ink)}

  /* ---------- NAV ---------- */
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    padding:22px 48px;
    display:flex;align-items:center;justify-content:space-between;
    transition:background .6s ease, padding .4s ease, backdrop-filter .6s;
  }
  .nav.scrolled{
    background:rgba(10,8,7,.78);
    backdrop-filter:blur(14px);
    padding:14px 48px;
    border-bottom:1px solid var(--line);
  }
  .nav .mark{
    font-family:'Italiana',serif;
    letter-spacing:.42em;
    font-size:18px;
    color:var(--cream);
  }
  .nav .mark span{color:var(--gold)}
  .nav ul{display:flex;gap:38px;list-style:none}
  .nav ul a{
    color:var(--cream);text-decoration:none;
    font-family:'Cormorant Garamond',serif;
    font-size:15px;letter-spacing:.18em;text-transform:uppercase;
    opacity:.78;transition:opacity .3s, color .3s;
  }
  .nav ul a:hover{opacity:1;color:var(--gold-bright)}
  .nav .cta{
    font-family:'Cormorant Garamond',serif;
    font-size:13px;letter-spacing:.22em;text-transform:uppercase;
    color:var(--ink);background:var(--gold);
    padding:10px 22px;border-radius:0;
    text-decoration:none;
    transition:background .3s;
  }
  .nav .cta:hover{background:var(--gold-bright)}

  /* ---------- HERO ---------- */
  .hero{
    position:relative;
    min-height:100vh;
    background:
      radial-gradient(ellipse at 70% 30%, rgba(90,26,31,.55), transparent 55%),
      radial-gradient(ellipse at 20% 80%, rgba(20,13,12,.9), transparent 60%),
      linear-gradient(180deg, #0a0807 0%, #140d0c 60%, #1d1311 100%);
    overflow:hidden;
    display:flex;align-items:center;
    padding:0 48px;
  }
  .hero::before{
    content:"";position:absolute;inset:0;
    background-image:
      radial-gradient(circle at 50% 50%, transparent 0%, rgba(0,0,0,.4) 100%);
    pointer-events:none;
  }
  .hero-grain{
    position:absolute;inset:0;opacity:.08;pointer-events:none;
    background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  .hero-inner{
    position:relative;z-index:3;
    display:grid;grid-template-columns:1fr 1fr;gap:60px;
    width:100%;max-width:1400px;margin:0 auto;
    align-items:center;
  }
  .hero-text{padding-top:60px}
  .hero-eyebrow{
    display:flex;align-items:center;gap:14px;
    font-family:'Cormorant Garamond',serif;
    font-size:13px;letter-spacing:.42em;text-transform:uppercase;
    color:var(--gold);margin-bottom:36px;
    opacity:0;animation:fadeUp 1s .3s forwards;
  }
  .hero-eyebrow::before{
    content:"";width:48px;height:1px;background:var(--gold);
  }
  .hero h1{
    font-family:'Cormorant Garamond',serif;
    font-weight:300;
    font-size:clamp(56px,7vw,104px);
    line-height:.98;
    letter-spacing:-.01em;
    color:var(--cream);
    margin-bottom:28px;
    opacity:0;animation:fadeUp 1.2s .5s forwards;
  }
  .hero h1 em{
    font-style:italic;font-weight:400;
    color:var(--gold-bright);
  }
  .hero h1 .cn{
    display:block;
    font-family:'Noto Serif SC',serif;
    font-size:.42em;letter-spacing:.3em;
    font-weight:300;color:var(--gold-dim);
    margin-top:18px;
  }
  .hero-sub{
    font-family:'Cormorant Garamond',serif;
    font-size:20px;font-style:italic;font-weight:300;
    color:rgba(243,234,216,.72);
    max-width:440px;margin-bottom:44px;
    line-height:1.5;
    opacity:0;animation:fadeUp 1s .8s forwards;
  }
  .hero-actions{
    display:flex;gap:24px;align-items:center;
    opacity:0;animation:fadeUp 1s 1.1s forwards;
  }
  .btn-primary{
    font-family:'Cormorant Garamond',serif;
    font-size:14px;letter-spacing:.28em;text-transform:uppercase;
    color:var(--ink);background:var(--gold);
    padding:16px 38px;text-decoration:none;
    transition:all .4s;
    border:1px solid var(--gold);
  }
  .btn-primary:hover{
    background:transparent;color:var(--gold-bright);
  }
  .btn-ghost{
    font-family:'Cormorant Garamond',serif;
    font-size:14px;letter-spacing:.28em;text-transform:uppercase;
    color:var(--cream);text-decoration:none;
    padding:16px 0;
    border-bottom:1px solid var(--line);
    transition:all .4s;
  }
  .btn-ghost:hover{color:var(--gold-bright);border-color:var(--gold)}

```html
  /* ---------- HERO VISUAL (signature: 花茎环) ---------- */
  .hero-visual{
    position:relative;
    height:78vh;min-height:560px;
    display:flex;align-items:center;justify-content:center;
    opacity:0;animation:fadeIn 1.4s .9s forwards;
  }
  .ring{
    position:absolute;
    width:520px;height:520px;
    border:1px solid var(--line);
    border-radius:50%;
    animation:spin 80s linear infinite;
  }
  .ring.r2{width:420px;height:420px;border-color:rgba(201,169,106,.12);animation-duration:60s;animation-direction:reverse}
  .ring.r3{width:620px;height:620px;border-color:rgba(201,169,106,.08);animation-duration:120s}
  .ring::before{
    content:"";position:absolute;top:-3px;left:50%;
    width:6px;height:6px;border-radius:50%;
    background:var(--gold-bright);
    box-shadow:0 0 14px var(--gold);
  }
  .bottle-silhouette{
    position:relative;z-index:2;
    width:280px;height:480px;
  }
  .bottle-silhouette svg{width:100%;height:100%;filter:drop-shadow(0 0 40px rgba(201,169,106,.25))}
  .petal-orbit{
    position:absolute;z-index:3;
    font-family:'Cormorant Garamond',serif;
    font-size:11px;letter-spacing:.3em;text-transform:uppercase;
    color:var(--gold);
  }
  .petal-orbit.top{top:8%}
  .petal-orbit.bot{bottom:8%}
  .petal-orbit .dot{
    display:inline-block;width:5px;height:5px;
    background:var(--amber);border-radius:50%;margin:0 8px;
    vertical-align:middle;
  }

  .hero-scroll{
    position:absolute;bottom:36px;left:50%;transform:translateX(-50%);
    z-index:5;
    font-family:'Cormorant Garamond',serif;
    font-size:11px;letter-spacing:.4em;text-transform:uppercase;
    color:var(--gold-dim);
    display:flex;flex-direction:column;align-items:center;gap:12px;
    opacity:0;animation:fadeIn 1s 1.6s forwards;
  }
  .hero-scroll::after{
    content:"";width:1px;height:48px;
    background:linear-gradient(180deg,var(--gold),transparent);
    animation:scrollLine 2.4s ease-in-out infinite;
  }

  @keyframes fadeUp{from{opacity:0;transform:translateY(28px)}to{opacity:1;transform:translateY(0)}}
  @keyframes fadeIn{from{opacity:0}to{opacity:1}}
  @keyframes spin{to{transform:rotate(360deg)}}
  @keyframes scrollLine{0%,100%{transform:scaleY(.4);transform-origin:top}50%{transform:scaleY(1);transform-origin:top}}

  /* ---------- SECTION COMMON ---------- */
  section{padding:140px 48px;position:relative}
  .container{max-width:1280px;margin:0 auto}
  .section-label{
    display:flex;align-items:center;gap:14px;
    font-family:'Cormorant Garamond',serif;
    font-size:12px;letter-spacing:.42em;text-transform:uppercase;
    color:var(--gold);margin-bottom:24px;
  }
  .section-label::before{content:"";width:32px;height:1px;background:var(--gold)}
  .section-label .num{color:var(--gold-dim);font-style:italic}
  .section-title{
    font-family:'Cormorant Garamond',serif;
    font-weight:300;
}
</style>
</html>
```