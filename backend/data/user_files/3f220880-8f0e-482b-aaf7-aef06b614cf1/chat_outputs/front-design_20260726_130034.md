我来为你设计一个高端大气的卖酒网站首页，采用深色奢华风格，配以金色点缀和精美的酒类配图。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MAISON NOIR — 臻藏佳酿</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --gold: #c9a961;
            --gold-light: #e0c887;
            --dark: #0a0a0a;
            --dark-2: #141414;
            --dark-3: #1c1c1c;
            --cream: #f5f0e8;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Noto Serif SC', serif;
            background: var(--dark);
            color: var(--cream);
            overflow-x: hidden;
        }
        .font-display { font-family: 'Cormorant Garamond', serif; }
        .text-gold { color: var(--gold); }
        .bg-gold { background-color: var(--gold); }
        .border-gold { border-color: var(--gold); }
        
        /* Hero背景 */
        .hero-bg {
            background: linear-gradient(135deg, rgba(10,10,10,0.85) 0%, rgba(10,10,10,0.4) 50%, rgba(10,10,10,0.9) 100%),
            url('https://images.unsplash.com/photo-1474722883778-792e7990302f?w=1920&q=80') center/cover;
        }
        
        /* 金色渐变文字 */
        .gold-text {
            background: linear-gradient(180deg, #e0c887 0%, #c9a961 50%, #a08442 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        /* 滚动动画 */
        .fade-up {
            opacity: 0;
            transform: translateY(40px);
            transition: all 1s ease;
        }
        .fade-up.visible {
            opacity: 1;
            transform: translateY(0);
        }
        
        /* 产品卡片hover */
        .product-card {
            transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
        }
        .product-card:hover {
            transform: translateY(-12px);
        }
        .product-card:hover .product-img {
            transform: scale(1.08);
        }
        .product-img {
            transition: transform 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        
        /* 金色分割线 */
        .gold-divider {
            width: 60px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
        }
        
        /* 导航hover */
        .nav-link {
            position: relative;
            transition: color 0.3s;
        }
        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 50%;
            width: 0;
            height: 1px;
            background: var(--gold);
            transition: all 0.3s;
        }
        .nav-link:hover { color: var(--gold); }
        .nav-link:hover::after { width: 100%; left: 0; }
        
        /* 按钮 */
        .btn-gold {
            background: linear-gradient(135deg, #c9a961, #a08442);
            color: #0a0a0a;
            transition: all 0.4s;
            position: relative;
            overflow: hidden;
        }
        .btn-gold::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.6s;
        }
        .btn-gold:hover::before { left: 100%; }
        .btn-gold:hover {
            box-shadow: 0 10px 40px rgba(201,169,97,0.3);
        }
        
        .btn-outline {
            border: 1px solid var(--gold);
            color: var(--gold);
            transition: all 0.4s;
        }
        .btn-outline:hover {
            background: var(--gold);
            color: var(--dark);
        }
        
        /* 滚动指示器 */
        @keyframes scrollDown {
            0% { transform: translateY(0); opacity: 1; }
            100% { transform:

使用的 Skill: front-design