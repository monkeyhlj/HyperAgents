<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON FLEUR — 花艺之屋</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600&family=Italiana&display=swap" rel="stylesheet">
<style>
  :root{
    --velvet:#1a0d14;
    --velvet-deep:#0f0709;
    --velvet-soft:#241420;
    --gold:#c9a55c;
    --gold-bright:#e6c98a;
    --gold-dim:#8a6f3d;
    --burgundy:#6b1f2c;
    --burgundy-bright:#8a2a3a;
    --amber:#d4843a;
    --cream:#f4ead8;
    --ink:#0a0508;
    --glass:rgba(244,234,216,0.04);
    --glass-border:rgba(201,165,92,0.22);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    background:var(--velvet-deep);
    color:var(--cream);
    font-family:'Noto Serif SC','Cormorant Garamond',serif;
    font-weight:300;
    line-height:1.7;
    overflow-x:hidden;
    -webkit-font-smoothing:antialiased;
  }
  ::selection{background:var(--gold);color:var(--ink)}

  /* ---------- grain & vignette overlay ---------- */
  body::before{
    content:"";
    position:fixed;inset:0;
    background:
      radial-gradient(ellipse at 50% 0%, transparent 40%, rgba(0,0,0,0.55) 100%),
      url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='200' height='200'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.8 0 0 0 0 0.65 0 0 0 0 0.36 0 0 0 0.08 0'/></filter><rect width='100%' height='100%' filter='url(%23n)'/></svg>");
    pointer-events:none;
    z-index:9999;
    mix-blend-mode:overlay;
    opacity:0.6;
  }

  /* ---------- slim nav ---------- */
  .nav{
    position:fixed;top:0;left:0;right:0;
    z-index:100;
    padding:22px 48px;
    display:flex;align-items:center;justify-content:space-between;
    backdrop-filter:blur(8px);
    background:linear-gradient(180deg,rgba(15,7,9,0.7),rgba(15,7,9,0));
    transition:padding .4s ease, background .4s ease;
  }
  .nav.scrolled{
    padding:14px 48px;
    background:rgba(15,7,9,0.85);
    border-bottom:1px solid var(--glass-border);
  }
  .nav__brand{
    font-family:'Italiana',serif;
    font-size:22px;
    letter-spacing:0.35em;
    color:var(--gold-bright);
  }
  .nav__brand span{color:var(--cream);font-weight:300}
  .nav__links{
    display:flex;gap:42px;list-style:none;
  }
  .nav__links a{
    color:var(--cream);
    text-decoration:none;
    font-family:'Cormorant Garamond',serif;
    font-size:13px;
    letter-spacing:0.28em;
    text-transform:uppercase;
    opacity:0.7;
    transition:opacity .3s ease,color .3s ease;
    position:relative;
  }
  .nav__links a::after{
    content:"";
    position:absolute;left:0;bottom:-6px;
    width:0;height:1px;background:var(--gold);
    transition:width .4s ease;
  }
  .nav__links a:hover{opacity:1;color:var(--gold-bright)}
  .nav__links a:hover::after{width:100%}
  .nav__cta{
    font-family:'Cormorant Garamond',serif;
    font-size:12px;letter-spacing:0.3em;text-transform:uppercase;
    color:var(--ink);
    background:var(--gold);
    padding:10px 22px;
    border:none;
    cursor:pointer;
    transition:all .3s ease;
  }
  .nav__cta:hover{background:var(--gold-bright);letter-spacing:0.36em}

  /* ---------- HERO — magazine cover ---------- */
  .hero{
    position:relative;
    min-height:100vh;
    display:grid;
    grid-template-columns:1fr 1fr;
    overflow:hidden;
    background:
      radial-gradient(ellipse at 70% 40%, var(--velvet-soft) 0%, var(--velvet) 45%, var(--velvet-deep) 100%);
  }
  .hero__left{
    position:relative;
    padding:140px 64px 80px 80px;
    display:flex;flex-direction:column;justify-content:center;
    z-index:3;
  }
  .hero__eyebrow{
    display:flex;align-items:center;gap:18px;
    font-family:'Cormorant Garamond',serif;
    font-size:12px;letter-spacing:0.42em;text-transform:uppercase;
    color:var(--gold);
    margin-bottom:36px;
  }
  .hero__eyebrow::before{
    content:"";width:48px;height:1px;background:var(--gold-dim);
  }
  .hero__title{
    font-family:'Italiana',serif;
    font-size:clamp(64px,9vw,128px);
    line-height:0.92;
    letter-spacing:-0.01em;
    color:var(--cream);
    margin-bottom:8px;
  }
  .hero__title em{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;
    font-weight:300;
    color:var(--gold-bright);
    display:block;
    font-size:0.42em;
    letter-spacing:0.04em;
    margin-top:14px;
  }
  .hero__sub{
    font-family:'Cormorant Garamond',serif;
    font-style:italic;
    font-size:20px;
    color:rgba(244,234,216,0.7);
    max-width:420px;
    margin:32px 0 48px;
    line-height:1.6;
  }
  .hero__meta{
    display:flex;gap:48px;
    border-top:1px solid var(--glass-border);
    padding-top:28px;
    max-width:440px;
  }
  .hero__meta-item span:first-child{
    display:block;
    font-family:'Cormorant Garamond',serif;
    font-size:11px;letter-spacing:0.3em;text-transform:uppercase;
    color:var(--gold-dim);
    margin-bottom:6px;
  }
  .hero__meta-item span:last-child{
    font-family:'Noto Serif SC',serif;
    font-size:15px;color:var(--cream);
  }

  /* signature composition — flower silhouette ring```css
  */
  .hero__right{
    position:relative;
    display:flex;align-items:center;justify-content:center;
    overflow:hidden;
  }
  .hero__ring{
    position:relative;
    width:min(560px,80%);
    aspect-ratio:1;
    display:flex;align-items:center;justify-content:center;
  }
  .hero__ring::before{
    content:"";
    position:absolute;inset:0;
    border:1px solid var(--glass-border);
    border-radius:50%;
    animation:spin 60s linear infinite;
  }
  .hero__ring::after{
    content:"";
    position:absolute;inset:8%;
    border:1px dashed rgba(201,165,92,0.18);
    border-radius:50%;
    animation:spin 90s linear infinite reverse;
  }
  @keyframes spin{to{transform:rotate(360deg)}}

  .hero__bloom{
    position:relative;
    width:62%;
    aspect-ratio:1;
    border-radius:50%;
    background:
      radial-gradient(circle at 50% 45%, rgba(214,132,58,0.35) 0%, rgba(107,31,44,0.5) 35%, transparent 70%),
      radial-gradient(circle at 50% 50%, var(--velvet) 0%, var(--velvet-deep) 80%);
    box-shadow:
      0 0 120px rgba(201,165,92,0.18),
      inset 0 0 80px rgba(0,0,0,0.6);
    display:flex;align-items:center;justify-content:center;
  }
  .hero__bloom svg{
    width:78%;height:78%;
    filter:drop-shadow(0 0 24px rgba(201,165,92,0.4));
  }
  .hero__petal{
    position:absolute;
    width:14px;height:46px;
    background:linear-gradient(180deg,var(--gold-bright),var(--gold-dim));
    border-radius:50% 50% 50% 50%/60% 60% 40% 40%;
    top:50%;left:50%;
    transform-origin:50% 100%;
    opacity:0.85;
  }

  /* orbiting labels */
  .hero__orbit{
    position:absolute;inset:0;
    animation:spin 80s linear infinite;
  }
  .hero__orbit-label{
    position:absolute;
    font-family:'Cormorant Garamond',serif;
    font-style:italic;
    font-size:13px;
    color:var(--gold);
    letter-spacing:0.15em;
    white-space:nowrap;
  }
  .hero__orbit-label:nth-child(1){top:2%;left:50%;transform:translateX(-50%)}
  .hero__orbit-label:nth-child(2){bottom:2%;left:50%;transform:translateX(-50%)}
  .hero__orbit-label:nth-child(3){top:50%;left:2%;transform:translateY(-50%) rotate(-90deg)}
  .hero__orbit-label:nth-child(4){top:50%;right:2%;transform:translateY(-50%) rotate(90deg)}

  .hero__scroll{
    position:absolute;
    bottom:36px;left:50%;
    transform:translateX(-50%);
    font-family:'Cormorant Garamond',serif;
    font-size:11px;letter-spacing:0.4em;text-transform:uppercase;
    color:var(--gold-dim);
    display:flex;flex-direction:column;align-items:center;gap:10px;
    z-index:5;
  }
  .hero__scroll::after{
    content:"";width:1px;height:40px;
    background:linear-gradient(180deg,var(--gold),transparent);
    animation:scrollPulse 2.4s ease-in-out infinite;
  }
  @keyframes scrollPulse{
    0%,100%{opacity:0.3;transform:scaleY(0.6)}
}
</style>
</html>
```