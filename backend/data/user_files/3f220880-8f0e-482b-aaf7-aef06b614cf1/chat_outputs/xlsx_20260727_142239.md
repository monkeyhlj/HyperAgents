```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR · 珍稀佳酿珍藏</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600;700&family=Jost:wght@200;300;400;500&display=swap" rel="stylesheet">
<style>
  :root{
    --velvet:#0a0608;
    --velvet-2:#14090c;
    --velvet-3:#1d0f13;
    --gold:#c9a96a;
    --gold-bright:#e8c987;
    --gold-deep:#8a6d3b;
    --burgundy:#5c1a1a;
    --burgundy-bright:#8b2a2a;
    --amber:#b87333;
    --ink:#f4ecd8;
    --ink-soft:rgba(244,236,216,0.72);
    --ink-faint:rgba(244,236,216,0.42);
    --line:rgba(201,169,106,0.22);
    --glass:rgba(20,9,12,0.55);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    background:var(--velvet);
    color:var(--ink);
    font-family:'Noto Serif SC','Cormorant Garamond',serif;
    font-weight:300;
    line-height:1.7;
    overflow-x:hidden;
    -webkit-font-smoothing:antialiased;
  }
  ::selection{background:var(--gold);color:var(--velvet)}

  /* ── Nav ─────────────────────────────────────────────── */
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    padding:1.4rem 3rem;
    display:flex;justify-content:space-between;align-items:center;
    background:linear-gradient(180deg,rgba(10,6,8,0.85) 0%,rgba(10,6,8,0) 100%);
    backdrop-filter:blur(6px);
    transition:padding .4s ease;
  }
  .nav.scrolled{padding:.9rem 3rem;background:rgba(10,6,8,0.92)}
  .nav__brand{
    font-family:'Cormorant Garamond',serif;
    font-size:1.35rem;letter-spacing:.42em;font-weight:500;
    color:var(--gold-bright);
  }
  .nav__brand span{color:var(--ink-faint);font-weight:300}
  .nav__links{display:flex;gap:2.6rem;list-style:none}
  .nav__links a{
    color:var(--ink-soft);text-decoration:none;
    font-family:'Jost',sans-serif;font-size:.72rem;
    letter-spacing:.28em;text-transform:uppercase;
    transition:color .3s;position:relative;
  }
  .nav__links a:hover{color:var(--gold-bright)}
  .nav__links a::after{
    content:'';position:absolute;left:0;bottom:-6px;
    width:0;height:1px;background:var(--gold);
    transition:width .4s ease;
  }
  .nav__links a:hover::after{width:100%}
  .nav__cta{
    font-family:'Jost',sans-serif;font-size:.7rem;
    letter-spacing:.28em;text-transform:uppercase;
    color```html
:var(--velvet);
    background:linear-gradient(135deg,var(--gold) 0%,var(--gold-deep) 100%);
    padding:.6rem 1.4rem;border:none;cursor:pointer;
    transition:transform .3s,box-shadow .3s;
  }
  .nav__cta:hover{transform:translateY(-1px);box-shadow:0 6px 20px rgba(201,169,106,0.35)}

  /* ── Hero ────────────────────────────────────────────── */
  .hero{
    position:relative;min-height:100vh;
    display:flex;align-items:center;justify-content:center;
    overflow:hidden;
    background:
      radial-gradient(ellipse at 50% 40%,rgba(92,26,26,0.35) 0%,transparent 60%),
      radial-gradient(ellipse at 80% 80%,rgba(184,115,51,0.12) 0%,transparent 50%),
      linear-gradient(180deg,var(--velvet) 0%,var(--velvet-2) 50%,var(--velvet) 100%);
  }
  .hero__grain{
    position:absolute;inset:0;opacity:.06;pointer-events:none;
    background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  .hero__arch{
    position:absolute;top:0;left:50%;transform:translateX(-50%);
    width:42vw;max-width:560px;height:100%;
    background:
      radial-gradient(ellipse 80% 55% at 50% 100%,rgba(92,26,26,0.4) 0%,transparent 70%),
      linear-gradient(180deg,transparent 0%,rgba(20,9,12,0.4) 40%,rgba(10,6,8,0.7) 100%);
    border-radius:50% 50% 0 0 / 38% 38% 0 0;
    border:1px solid var(--line);
    border-bottom:none;
  }
  .hero__arch::before{
    content:'';position:absolute;top:8%;left:50%;transform:translateX(-50%);
    width:70%;height:1px;
    background:linear-gradient(90deg,transparent,var(--gold),transparent);
    opacity:.4;
  }
  .hero__bottle{
    position:absolute;top:50%;left:50%;transform:translate(-50%,-42%);
    width:14vw;max-width:180px;min-width:120px;
    z-index:2;
    filter:drop-shadow(0 30px 40px rgba(0,0,0,0.7));
    animation:floatBottle 6s ease-in-out infinite;
  }
  @keyframes floatBottle{
    0%,100%{transform:translate(-50%,-42%)}
    50%{transform:translate(-50%,-45%)}
  }
  .hero__bottle svg{width:100%;height:auto}
  .hero__content{
    position:relative;z-index:3;text-align:center;
    padding:0 2rem;max-width:900px;
  }
  .hero__eyebrow{
    font-family:'Jost',sans-serif;font-size:.72rem;
    letter-spacing:.5em;text-transform:uppercase;
    color:var(--gold);margin-bottom:2rem;
    opacity:0;animation:fadeUp 1s .3s forwards;
  }
  .hero__title{
    font-family:'Cormorant Garamond',serif;
    font-size:clamp(3rem,9vw,7rem);font-weight:300;line-height:1.05;letter-spacing:.02em;margin-bottom:1.5rem;opacity:0;animation:fadeUp 1.2s .5s forwards}.hero__title em{font-style:italic;color:var(--gold-bright);font-weight:400}.hero__title span{display:block;font-size:clamp(1.8rem,5vw,3.5rem);color:var(--ink-soft);font-weight:300;letter-spacing:.08em}.hero__sub{font-family:'Noto Serif SC',serif;font-size:1.05rem;color:var(--ink-soft);max-width:560px;margin:0 auto 3rem;line-height:1.9;font-weight:300;opacity:0;animation:fadeUp 1s .8s forwards}.hero__meta{display:flex;justify-content:center;gap:3rem;flex-wrap:wrap;opacity:0;animation:fadeUp 1s 1.1s forwards}.hero__meta-item{text-align:center}.hero__meta-num{font-family:'Cormorant Garamond',serif;font-size:2.2rem;color:var(--gold-bright);font-weight:400;display:block}.hero__meta-label{font-family:'Jost',sans-serif;font-size:.62rem;letter-spacing:.3em;text-transform:uppercase;color:var(--ink-faint);margin-top:.3rem}.hero__scroll{position:absolute;bottom:2.5rem;left:50%;transform:translateX(-50%);z-index:3;text-align:center;opacity:0;animation:fadeUp 1s 1.5s forwards}.hero__scroll-text{font-family:'Jost',sans-serif;font-size:.62rem;letter-spacing:.35em;text-transform:uppercase;color:var(--ink-faint);display:block;margin-bottom:.8rem}.hero__scroll-line{width:1px;height:50px;background:linear-gradient(180deg,var(--gold),transparent);margin:0 auto;animation:scrollPulse 2s ease-in-out infinite}@keyframes scrollPulse{0%,100%{opacity:.3;transform:scaleY(.7)}50%{opacity:1;transform:scaleY(1)}}@keyframes fadeUp{from{opacity:0;transform:translateY(30px)}to{opacity:1;transform:translateY(0)}}

  /* ── Section common ──────────────────────────────────── */
  .section{padding:8rem 3rem;position:relative}
  .section__label{font-family:'Jost',sans-serif;font-size:.68rem;letter-spacing:.45em;text-transform:uppercase;color:var(--gold);margin-bottom:1.5rem;display:flex;align-items:center;gap:1rem}.section__label::before{content:'';width:40px;height:1px;background:var(--gold)}.section__heading{font-family:'Cormorant Garamond',serif;font-size:clamp(2.2rem,5vw,4rem);font-weight:300;line-height:1.15;margin-bottom:2rem;letter-spacing:.01em}.section__heading em{font-style:italic;color:var(--gold-bright)}

  /* ── Collection ──────────────────────────────────────── */
}
</style>
</html>