我来为你设计一个高端大气的卖酒网站首页。我会使用深色奢华风格，配以金色点缀，营造高端酒庄的质感。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON NOIR — 臻藏佳酿 · 时光之选</title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=Noto+Serif+SC:wght@300;400;500;600;700&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0807;
    --bg-2: #14100c;
    --bg-3: #1c1612;
    --gold: #c9a96a;
    --gold-light: #e8d4a8;
    --gold-deep: #8a6f3e;
    --cream: #f5ede0;
    --muted: #8a7d6b;
    --line: rgba(201, 169, 106, 0.18);
  }
  * { -webkit-font-smoothing: antialiased; }
  html { scroll-behavior: smooth; }
  body {
    background: var(--bg);
    color: var(--cream);
    font-family: 'Noto Serif SC', 'Cormorant Garamond', serif;
    overflow-x: hidden;
  }
  .font-display { font-family: 'Cormorant Garamond', 'Noto Serif SC', serif; }
  .font-sans { font-family: 'Jost', sans-serif; }
  .text-gold { color: var(--gold); }
  .text-gold-light { color: var(--gold-light); }
  .text-cream { color: var(--cream); }
  .text-muted { color: var(--muted); }
  .bg-bg-2 { background: var(--bg-2); }
  .bg-bg-3 { background: var(--bg-3); }
  .border-line { border-color: var(--line); }

  /* 金色渐变文字 */
  .gold-text {
    background: linear-gradient(135deg, #e8d4a8 0%, #c9a96a 45%, #8a6f3e 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Hero 背景 */
  .hero-bg {
    background:
      radial-gradient(ellipse at 20% 30%, rgba(201,169,106,0.12) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 70%, rgba(138,111,62,0.08) 0%, transparent 55%),
      linear-gradient(180deg, #0a0807 0%, #14100c 50%, #0a0807 100%);
  }

  /* 噪点纹理 */
  .noise::before {
    content: '';
    position: absolute; inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.4'/%3E%3C/svg%3E");
```html
    opacity: 0.06;
    pointer-events: none;
    mix-blend-mode: overlay;
  }

  /* 金线分隔 */
  .gold-rule {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold) 50%, transparent);
  }

  /* 瓶身光晕 */
  .bottle-glow {
    filter: drop-shadow(0 25px 50px rgba(201,169,106,0.25)) drop-shadow(0 0 30px rgba(0,0,0,0.6));
  }

  /* 浮动动画 */
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-14px) rotate(0.5deg); }
  }
  .float-slow { animation: float 7s ease-in-out infinite; }

  /* 旋转印章 */
  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .spin-slow { animation: spin-slow 40s linear infinite; }

  /* 入场 */
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity 1.1s cubic-bezier(.2,.7,.2,1), transform 1.1s cubic-bezier(.2,.7,.2,1); }
  .reveal.in { opacity: 1; transform: translateY(0); }

  /* 按钮 */
  .btn-gold {
    position: relative;
    background: linear-gradient(135deg, #e8d4a8, #c9a96a 50%, #8a6f3e);
    color: #14100c;
    overflow: hidden;
    transition: transform .4s ease, box-shadow .4s ease;
  }
  .btn-gold::after {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.5) 50%, transparent 70%);
    transform: translateX(-120%);
    transition: transform .8s ease;
  }
  .btn-gold:hover { transform: translateY(-2px); box-shadow: 0 14px 40px rgba(201,169,106,0.35); }
  .btn-gold:hover::after { transform: translateX(120%); }

  .btn-ghost {
    border: 1px solid var(--line);
    color: var(--cream);
    transition: all .4s ease;
  }
  .btn-ghost:hover { border-color: var(--gold); color: var(--gold-light); background: rgba(201,169,106,0.06); }

  /* 卡片 */
  .card-vin {
    background: linear-gradient(180deg, rgba(28,22,18,0.9), rgba(20,16,12,0.6));
    border: 1px solid var(--line);
    transition: transform .6s cubic-bezier(.2,.7,.2,1), border-color .6s ease, box-shadow .6s ease;
  }
  .card-vin:hover {
    transform: translateY(-8px);
    border-color: rgba(201,169,106,0.5);
    box-shadow: 0 30px 60px -20px rgba(0,0,0,0.8), 0 0 0 1px rgba(201,169,106,0.15);
  }

  /* 标签角标 */
  .tag-vintage {
    background: linear-gradient(135deg, rgba(201,169,106,0.15), rgba(201,169,106,0.05));
    border: 1px solid rgba(201,169,106,0.3);
  }

  /* 滚动指示 */
  @keyframes scrollDot {
    0% { transform: translateY(0); opacity: 0; }
    30% { opacity: 1; }
   ```html
100% { transform: translateY(18px); opacity: 0; }
  }
  .scroll-dot { animation: scrollDot 2s ease-in-out infinite; }

  /* 蒸汽/光雾 */
  .mist {
    background: radial-gradient(ellipse at center, rgba(201,169,106,0.15) 0%, transparent 70%);
    filter: blur(40px);
  }

  /* 数字标号 */
  .num-roman { font-family: 'Cormorant Garamond', serif; font-style: italic; }

  /* 滚动条 */
  ::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: #0a0807; }
  ::-webkit-scrollbar-thumb { background: linear-gradient(180deg, #8a6f3e, #c9a96a); border-radius: 4px; }

  /* 选区 */
  ::selection { background: rgba(201,169,106,0.3); color: #f5ede0; }
</style>
</head>
<body class="relative">

<!-- ============ NAV ============ -->
<header class="fixed top-0 left-0 right-0 z-50 transition-all duration-500" id="nav">
  <div class="max-w-[1400px] mx-auto px-6 lg:px-10 py-5 flex items-center justify-between">
    <a href="#" class="flex items-center gap-3 group">
      <svg width="34" height="34" viewBox="0 0 40 40" fill="none" class="transition-transform duration-500 group-hover:rotate-180">
        <circle cx="20" cy="20" r="18" stroke="url(#g1)" stroke-width="1.2"/>
        <path d="M20 8 L20 32 M14 14 L26 26 M26 14 L14 26" stroke="url(#g1)" stroke-width="0.8" opacity="0.6"/>
        <defs><linearGradient id="g1" x1="0" y1="0" x2="40" y2="40"><stop stop-color="#e8d4a8"/><stop offset="1" stop-color="#8a6f3e"/></linearGradient></defs>
      </svg>
      <div class="leading-none">
        <div class="font-display text-xl tracking-[0.3em] text-cream">MAISON NOIR</div>
        <div class="font-sans text-[10px] tracking-[0.4em] text-gold mt-1">EST · 1892</div>
      </div>
    </a>
    <nav class="hidden lg:flex items-center gap-10 font-sans text-[13px] tracking-[0.2em] uppercase">
      <a href="#collection" class="text-cream/80 hover:text-gold-light transition">臻藏系列</a>
      <a href="#story" class="text-cream/80 hover:text-gold-light transition">酒庄故事</a>
      <a href="#ritual" class="text-cream/80 hover:text-gold-light transition">品鉴之道</a>
      <a href="#contact" class="text-cream/80 hover:text-gold-light transition">私享订制</a>
    </nav>
    <div class="flex items-center gap-4">
      <button class="hidden md:flex items-center gap-2 font-sans text-[12px] tracking-[0.2em] uppercase text-cream/70 hover:text-gold-light transition">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.3-4.3"/></svg>
        搜寻
      </button>
      <a href="#contact" class="btn-gold font-sans text-[12px] tracking-[0.2em] uppercase px-5 py-2.5 rounded-full">预约品鉴

使用的 Skill: front-design