<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MAISON FLEUR — 花之邸 · 高定花艺</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Noto+Serif+SC:wght@300;400;500;600&family=Italiana&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0a0606;
    --velvet:#14090b;
    --velvet-2:#1d0e11;
    --burgundy:#5a1622;
    --burgundy-deep:#3a0d16;
    --rose:#b8526a;
    --rose-pale:#e8c5cf;
    --gold:#c9a24a;
    --gold-bright:#e6c878;
    --gold-soft:#8a6f33;
    --cream:#f3e9d8;
    --paper:#ece2cf;
    --line: rgba(201,162,74,0.22);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html,body{background:var(--velvet);color:var(--cream);font-family:"Noto Serif SC","Cormorant Garamond",serif;-webkit-font-smoothing:antialiased;overflow-x:hidden}
  ::selection{background:var(--gold);color:var(--ink)}
  a{color:inherit;text-decoration:none}
  img{display:block;max-width:100%}

  /* ---------- NAV ---------- */
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:50;
    display:flex;align-items:center;justify-content:space-between;
    padding:22px 48px;
    background:linear-gradient(180deg, rgba(10,6,6,0.55), rgba(10,6,6,0));
    backdrop-filter:blur(6px);
  }
  .nav .mark{
    font-family:"Italiana",serif;letter-spacing:.42em;font-size:15px;color:var(--gold-bright);
  }
  .nav .mark span{color:var(--rose-pale);font-style:italic;letter-spacing:.1em}
  .nav ul{display:flex;gap:38px;list-style:none;font-size:12px;letter-spacing:.32em;text-transform:uppercase;color:var(--paper)}
  .nav ul li{position:relative;padding:6px 0;cursor:pointer;transition:color .4s}
  .nav ul li:hover{color:var(--gold-bright)}
  .nav ul li::after{content:"";position:absolute;left:0;bottom:0;height:1px;width:0;background:var(--gold);transition:width .5s}
  .nav ul li:hover::after{width:100%}
  .nav .cta-mini{
    font-size:11px;letter-spacing:.3em;text-transform:uppercase;
    border:1px solid var(--gold-soft);padding:9px 18px;color:var(--gold-bright);
    transition:.4s;cursor:pointer
  }
  .nav .cta-mini:hover{background:var(--gold);color:var(--ink);border-color:var(--gold)}

  /* ---------- HERO ---------- */
  .hero{
    position:relative;min-height:100vh;width:100%;
    background:
      radial-gradient(120% 80% at 70% 20%, rgba(90,22,34,0.55), transparent 60%),
      radial-gradient(80% 60% at 10% 90%, rgba(20,9,11,0.9), transparent 70%),
      linear-gradient(180deg, #00a0606 0%, #14090b 45%, #1d0e11 100%);
    overflow:hidden;
  }
  .hero::before{
    content:"";position:absolute;inset:0;
    background-image:
      radial-gradient(1px 1px at 20% 30%, rgba(230,200,120,0.4), transparent),
      radial-gradient(1px 1px at 80% 70%, rgba(232,197,207,0.3), transparent),
      radial-gradient(1px 1px at 50% 50%, rgba(201,162,74,0.2), transparent);
    background-size: 200px 200px, 300px 300px, 250px 250px;
    opacity:.6;pointer-events:none;
  }
  .hero-grain{
    position:absolute;inset:0;opacity:.08;pointer-events:none;mix-blend-mode:overlay;
    background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }

  .hero-inner{
    position:relative;z-index:3;
    display:grid;grid-template-columns:1.1fr 0.9fr;
    min-height:100vh;align-items:center;
    padding:120px 64px 80px;
    gap:40px;
  }

  .hero-left{position:relative}
  .eyebrow{
    display:inline-flex;align-items:center;gap:14px;
    font-size:11px;letter-spacing:.5em;text-transform:uppercase;color:var(--gold);
    margin-bottom:36px;
  }
  .eyebrow .dash{width:48px;height:1px;background:var(--gold-soft)}
  .hero h1{
    font-family:"Cormorant Garamond",serif;
    font-weight:300;
    font-size:clamp(56px, 8.4vw, 132px);
    line-height:.92;letter-spacing:-.02em;
    color:var(--cream);
    margin-bottom:8px;
  }
  .hero h1 .it{
    font-style:italic;font-weight:300;color:var(--rose-pale);
    display:block;
  }
  .hero h1 .gold-line{
    display:block;color:var(--gold-bright);
  }
  .hero .zh-title{
    font-family:"Noto Serif SC",serif;font-weight:300;
    font-size:clamp(18px,2vw,26px);letter-spacing:.6em;
    color:var(--paper);margin:28px 0 32px;padding-left:4px;
    opacity:.85;
  }
  .hero .lede{
    max-width:440px;font-size:15px;line-height:1.9;color:rgba(243,233,216,0.72);
    font-weight:300;letter-spacing:.02em;
    border-left:1px solid var(--line);padding-left:22px;
  }
  .hero-actions{display:flex;gap:18px;margin-top:46px;align-items:center}
  .btn-gold{
    display:inline-flex;align-items:center;gap:12px;
    background:linear-gradient(135deg, var(--gold-bright), var(--gold));
    color:var(--ink);font-size:12px;letter-spacing:.3em;text-transform:uppercase;
    padding:16px 30px;font-weight:500;
    transition:.5s;cursor:pointer;border:none;
  }
  .btn-gold:hover{background:var(--cream);transform:translateY(-2px);box-shadow:0 12px 40px rgba(201,162,74,0.35```css
)}
  .btn-ghost{
    display:inline-flex;align-items:center;gap:10px;
    color:var(--paper);font-size:12px;letter-spacing:.3em;text-transform:uppercase;
    padding:16px 0;font-weight:300;cursor:pointer;
    border-bottom:1px solid var(--line);transition:.4s;
  }
  .btn-ghost:hover{color:var(--gold-bright);border-color:var(--gold-bright)}
  .btn-ghost svg{width:14px;height:14px;transition:transform .4s}
  .btn-ghost:hover svg{transform:translateX(6px)}

  /* signature: bloom ring */
  .hero-right{position:relative;display:flex;align-items:center;justify-content:center}
  .bloom-ring{
    position:relative;width:min(520px,42vw);aspect-ratio:1;
  }
  .bloom-ring .ring{
    position:absolute;inset:0;border:1px solid var(--line);border-radius:50%;
    animation:spin 60s linear infinite;
  }
  .bloom-ring .ring.r2{inset:8%;border-color:rgba(232,197,207,0.14);animation-duration:90s;animation-direction:reverse}
  .bloom-ring .ring.r3{inset:18%;border-color:rgba(201,162,74,0.18);animation-duration:120s}
  .bloom-ring .ring .tick{
    position:absolute;width:1px;height:10px;background:var(--gold-soft);
    left:50%;top:0;transform-origin:50% 50%;
  }
  .bloom-ring .ring .dot{
    position:absolute;width:6px;height:6px;border-radius:50%;background:var(--gold-bright);
    left:50%;top:0;transform:translate(-50%,-50%);
    box-shadow:0 0 12px var(--gold-bright);
  }
  .bloom-ring .ring .label{
    position:absolute;font-size:9px;letter-spacing:.4em;text-transform:uppercase;
    color:var(--gold);font-family:"Italiana",serif;
  }
  .bloom-ring .ring .label.l1{top:6%;left:50%;transform:translateX(-50%)}
  .bloom-ring .ring .label.l2{bottom:6%;left:50%;transform:translateX(-50%)}
  .bloom-ring .ring .label.l3{left:4%;top:50%;transform:translateY(-50%) rotate(-90deg)}
  .bloom-ring .ring .label.l4{right:4%;top:50%;transform:translateY(-50%) rotate(90deg)}

  .bloom-core{
    position:absolute;inset:26%;border-radius:50%;
    background:
      radial-gradient(circle at 40% 35%, rgba(232,197,207,0.35), transparent 55%),
      radial-gradient(circle at 60% 70%, rgba(184,82,106,0.4), transparent 60%),
      radial-gradient(circle at 50% 50%, rgba(201,162,74,0.18), transparent 70%),
      var(--burgundy-deep);
    box-shadow:
      inset 0 0 60px rgba(0,0,0,0.6),
      inset 0 0 20px rgba(184,82,106,0.3),
      0 0 80px rgba(90,22,34,0.5);
    display:flex;align-items:center;justify-content:center;
  }
  .bloom-core .petal-svg{
    width:80%;height:80%;opacity:.85;
    filter:drop-shadow(0 0 18px rgba(230,200,120,0.3));
  }
  .bloom-core .seal{
    position:absolute;font-family:"Italiana",serif;
    font-size:10px;letter-spacing:.5em;color:var(--gold-bright);
    bottom:-8%;text-transform:uppercase;
  }
}
</style>
</html>
```